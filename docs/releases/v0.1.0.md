# Neil Agent v0.1.0

**功能收口 / 维护期起点。** Git tag：`v0.1.0`（commit `0b13c05`）。包版本 `0.1.0`（`pyproject.toml` / `__version__`）。

默认进入维护：修缺陷、依赖与文档，不扩能力面。可选批次（Session goals、Plan DSL）不在本 tag。

## 包含

- 入口：`neil-agent`、`neil-agent -p`、`neil-agent-eval`、`neil-agent-web`
- Provider：DeepSeek、Claude、OpenAI、Ollama、vLLM
- 安全加固批次 1–6（denylist、审批绑定、Git 脱敏、写入 `O_NOFOLLOW` 等）
- 运行时预设 1–3、5：`RuntimeProfile`、可逆注册、只读子任务、`load_skill`
- Web Workbench P0–P9；TUI 可视化 Phase 0A–4
- 三入口 `host_runtime`；Windows Sandbox 契约（通用命令默认关闭）

## 不在本 tag

- Session goals（批次 4）、Plan DSL（批次 6）
- GitHub Actions 通用 CI（本地 `.github/workflows/ci.yml` 因 token 缺 `workflow` 未推送）
- Web 独立子任务面板

## 使用

Python ≥ 3.13。`uv run neil-agent` / `uv run neil-agent-web`。状态见 [`docs/project-status.md`](../project-status.md)。
