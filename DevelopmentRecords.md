# Neil Agent 开发记录

## 项目信息

- 项目名称：Neil Agent
- 项目类型：本地 Coding Agent / AI 编程助手
- 开发语言：Python
- 开发环境：VS Code
- 项目负责人：Neil
- 文档创建时间：2026 年 7 月
- GitHub 仓库：https://github.com/zyohhh/Neil-Agent
- 当前版本：v0.1.0-dev
- 当前状态：真实 v1/v2 协议验收已通过；OS 沙箱契约与 Windows fail-closed 能力门禁已接入，通用命令仍保持关闭

## 文件职责

| 文件 | 当前职责 |
| --- | --- |
| `cli.py` | 通过可注入 Console 接收输入、展示流式文本并处理终端命令 |
| `agent.py` | 管理多轮历史、压缩检查点、受限工具循环和修改后验证工作流 |
| `context.py` | 估算上下文、选择完整历史轮次并保存待应用的压缩结果 |
| `diagnostics.py` | 为 `/doctor` 执行不联网、不泄露密钥的本地环境与 OS 沙箱能力检查 |
| `instructions.py` | 分层加载、热重载并非覆盖式初始化有界 `AGENTS.md` 项目指令 |
| `evals.py` | 运行默认离线评测和双重显式开启的真实 DeepSeek 验收 |
| `llm.py` | 封装 DeepSeek 请求、可观察重试、流式事件、工具调用和服务端用量解析 |
| `noninteractive.py` | 执行一次性只读请求并输出版本化 text/json/JSONL 协议 |
| `hooks.py` | 提供有界、类型化的模型与工具生命周期回调 |
| `audit.py` | 使用生命周期 hooks 写入元数据专用的有界本地 JSONL 审计 |
| `checkpoint.py` | 按 Agent 回合保存本进程多文件编辑的有界任务恢复栈和内容身份 |
| `errors.py` | 定义应用、Agent、LLM 和工具层的用户可见异常层级 |
| `schemas.py` | 定义消息、思考内容、工具定义、调用、结果和模型响应结构 |
| `task.py` | 管理单次任务计划、步骤状态和最近一次质量检查快照 |
| `session.py` | 迁移、命名、搜索、原子保存和恢复工作区内的版本化会话快照 |
| `sandbox.py` | 定义不可变 OS 沙箱契约、Windows 能力探测、安全配置和 fail-closed 执行边界 |
| `config.py` | 从环境变量或 `.env` 读取并校验 API Key、模型、提示词、重试和运行限制 |
| `registry.py` | 注册工具、生成预览、执行审批检查并统一返回结果 |
| `filesystem.py` | 在工作区内读取、搜索以及原子写入 UTF-8 文件 |
| `sensitive_paths.py` | 共享凭据目录/文件 denylist，供文件工具、Git、Web、guest export 与沙箱快照使用 |
| `shell.py` | 执行固定质量检查、只读 Git，以及需审批的明确路径暂存和本地提交 |
| `__init__.py` | 保存包版本，并把命令行入口转发给 `cli.py` |

## 第一阶段目标

完成一个不包含工具调用的最小聊天闭环：

```text
用户输入
   ↓
cli.py 处理终端交互
   ↓
agent.py 组织最近的多轮消息
   ↓
llm.py 调用 DeepSeek 并返回流式文本
   ↓
cli.py 实时展示回答
   ↓
agent.py 在回答完整结束后保存本轮消息
```

## 2026-07-13：第一个可用版本

### 已完成

- 使用 `pydantic-settings` 从 `.env` 加载配置。
- 默认模型设置为 `deepseek-v4-flash`。
- 使用 DeepSeek Anthropic 兼容地址 `https://api.deepseek.com/anthropic`。
- 使用 `SecretStr` 保存 API Key，避免配置对象意外输出明文密钥。
- 定义不可变的 `Message`、`ToolCall` 和 `ToolResult` 数据结构。
- 实现普通模型请求 `LLMClient.complete()`。
- 实现流式模型请求 `LLMClient.stream()`。
- 将常见的鉴权、限流、超时、连接和 HTTP 错误转换为中文提示。
- 实现 `Agent.chat()` 和 `Agent.stream_chat()`。
- 只在一次回答完整结束后写入消息历史；失败或中断的回答不会污染历史。
- 根据 `MAX_ROUNDS` 仅向模型发送最近的对话轮次，默认保留 20 轮。
- 实现终端多轮聊天，以及 `/help`、`/clear`、`/exit` 命令。
- 修正包入口，现在可以通过 `neil-agent` 启动终端程序。

### 当前配置

将 `.env.example` 复制为 `.env`，然后填写真实 API Key：

```dotenv
DEEPSEEK_API_KEY=your_real_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com/anthropic
DEEPSEEK_MODEL=deepseek-v4-flash
THINKING_ENABLED=false
SYSTEM_PROMPT="You are Neil Agent, a helpful local coding assistant. Give accurate, practical, and concise answers."
MAX_TOKENS=8192
MAX_ROUNDS=20
MAX_CONTEXT_CHARS=120000
MAX_TOOL_ROUNDS=5
REQUEST_TIMEOUT=120
MAX_RETRIES=2
RETRY_BASE_DELAY=1
RETRY_MAX_DELAY=8
WORKSPACE_ROOT=.
COMMAND_TIMEOUT=120
MAX_COMMAND_OUTPUT_CHARS=20000
```

`THINKING_ENABLED` 默认是 `false`，让响应更直接、流式展示更及时。设置为 `true` 后，普通请求和流式请求都会启用 DeepSeek 思考模式。`SYSTEM_PROMPT` 用于调整 Agent 的角色和回答方式，不再需要修改 Python 代码。

### 启动方式

```powershell
Copy-Item .env.example .env
# 编辑 .env 并填写真实的 DEEPSEEK_API_KEY
uv run neil-agent
```

终端内可用命令：

```text
/help   显示帮助
/clear  清空对话历史
/status 显示任务计划、最近检查和本地 Git 状态
/exit   退出程序
```

### 验证结果

- Ruff 代码检查：通过
- Ruff 格式检查：通过
- mypy 类型检查：通过（13 个源文件）
- pytest 离线单元测试：62 项通过
- 单元测试不会发送真实 DeepSeek 请求，也不会消耗 API 额度

2026-07-14，Neil 已使用真实 API Key 完成端到端聊天测试，确认配置加载、网络请求、DeepSeek V4 Flash、流式输出和多轮消息链路均可用。

## 2026-07-14：可配置系统提示词和思考模式

### 已完成

- 增加 `SYSTEM_PROMPT` 配置，可通过 `.env` 修改 Agent 的系统指令。
- 拒绝空白系统提示词，避免向模型发送无效配置。
- 增加 `THINKING_ENABLED` 布尔配置，默认关闭。
- 普通请求和流式请求共享同一套思考模式配置。
- 开启思考模式时发送 Anthropic 兼容的 `thinking` 参数。
- CLI 启动信息会显示当前思考模式是开启还是关闭。
- 增加系统提示词、思考模式和配置校验测试。

## 2026-07-14：只读工具调用闭环

### 运行流程

```text
用户提出项目问题
   ↓
DeepSeek 返回 ToolCall
   ↓
ToolRegistry 校验名称和参数
   ↓
执行 list_directory / read_file / search_text
   ↓
Agent 将 ToolResult 返回 DeepSeek
   ↓
DeepSeek 继续调用工具或生成最终回答
```

### 已完成

- 实现 `list_directory`、`read_file` 和 `search_text` 三个只读工具。
- 使用 `WORKSPACE_ROOT` 限制可访问的项目根目录。
- 解析真实路径，防止利用 `..`、绝对路径或符号链接逃出工作区。
- 默认隐藏 `.env`、`.git`、虚拟环境、缓存目录和常见私钥文件。
- 单文件读取上限为 1 MB，搜索结果上限为 100 条。
- 实现工具注册、重复名称检查、参数绑定、异常捕获和统一 `ToolResult`。
- 将 Anthropic 格式的工具定义、`tool_use` 和 `tool_result` 接入 DeepSeek。
- 思考模式执行工具时保留并回传必要的 thinking 内容。
- 使用 `MAX_TOOL_ROUNDS` 限制单次用户请求的工具循环，默认最多 5 轮。
- 工具循环完整成功后才写入消息历史；失败不会留下半轮历史。
- CLI 启动时显示工作区和已注册的只读工具数量。
- 新增文件权限、注册表、工具循环和 API 消息转换测试。

## 2026-07-14：异常分层与 CLI 可测试性

- 新增 `NeilAgentError` 作为用户可见错误的共同基类。
- `AgentError`、`LLMError` 和 `ToolError` 分别归属编排层、模型层和工具层。
- Agent 不再依赖或误用 `LLMError` 表示自身循环错误。
- CLI 统一捕获 `NeilAgentError`，仍向用户显示简洁的中文错误。
- 移除模块级 `Console` 实例，由 `main()` 创建并传给 `run(console)`。
- 所有 CLI 展示函数均显式接收 Console，便于替换输入和捕获输出。
- 增加异常继承关系和注入式 CLI 交互测试。

## 2026-07-15：受控文件修改

### 审批流程

```text
DeepSeek 请求 write_file / replace_text
   ↓
ToolRegistry 生成 unified diff
   ↓
CLI 展示修改预览并等待用户输入 y/yes
   ├─ 拒绝：不修改文件，将拒绝结果返回模型
   └─ 批准：执行原子写入，再将结果返回模型
```

### 已完成

- 新增 `write_file`，支持创建或完整替换 UTF-8 文本文件。
- 新增 `replace_text`，只有实际匹配数量等于 `expected_replacements` 时才执行。
- 写工具在注册时标记为必须审批，并且必须提供预览函数。
- 未审批、无法审批或用户拒绝时，写工具不会执行。
- CLI 只将明确的 `y` 或 `yes` 视为批准，其他输入默认拒绝。
- 使用 unified diff 展示修改内容，预览最多 20,000 字符。
- 预览包含内容 `Change-ID`；文件在确认后发生变化时拒绝使用旧审批。
- 使用同目录临时文件、刷新磁盘和 `os.replace` 实现原子写入。
- 写入失败时清理临时文件并保留原文件内容。
- 继续复用工作区边界、敏感文件屏蔽和 1 MB 文件大小限制。
- CLI 当前注册 5 个工具，其中 2 个写工具必须人工确认。
- 增加批准、拒绝、精确匹配、敏感路径和模拟写入失败测试。

2026-07-16，Neil 已使用真实 DeepSeek API 验证 `write_file` / `replace_text` 的写入预览、批准和拒绝流程，确认审批闭环可用。

## 2026-07-16：受限命令执行

### 权限规则

- `run_quality_check` 只接受 `pytest`、`ruff`、`mypy` 三个枚举值，并在执行前展示固定命令、工作目录和超时时间。
- 质量检查器的配置或插件可能运行项目代码，因此 `run_quality_check` 必须经过人工批准。
- `git_status` 和 `git_diff` 只读取仓库状态，不修改工作区，可直接执行；同时禁用分页器、fsmonitor、external diff、textconv 和可选锁。
- 不提供任意命令、任意参数、管道、重定向或 Shell 字符串入口。

### 已完成

- 使用当前 Python 解释器的 `-m` 形式运行质量检查，确保复用 Neil Agent 的虚拟环境。
- 所有命令都固定在 `WORKSPACE_ROOT` 中运行，并显式设置 `shell=False` 和空标准输入。
- 子进程只继承运行所需的少量环境变量，不传递 DeepSeek API Key、GitHub Token 等凭据。
- 使用 `COMMAND_TIMEOUT` 限制单次命令时间，默认 120 秒。
- 使用 `MAX_COMMAND_OUTPUT_CHARS` 限制返回给模型的输出，默认 20,000 字符；超长输出保留开头和结尾。
- 命令不存在、超时、启动失败和非零退出码统一转换为工具错误。
- CLI 当前注册 8 个工具，其中文件写入和质量检查共 3 个工具必须人工确认。
- 增加白名单、审批、只读 Git、工作目录、环境净化、超时和输出截断测试。

2026-07-17，Neil 已使用真实 DeepSeek API 验证质量检查的预览、批准和拒绝，以及 `git_status` / `git_diff` 只读调用，确认受限命令闭环可用。

## 2026-07-17：主动质量验证和本地 Git 保存

### 修改后验证

- Agent 在文件写入成功后会明确提醒模型根据修改类型选择合适的 `run_quality_check`，而不是无理由运行全部检查。
- 内置工具工作流会追加到可配置系统提示词之后，不会替换用户在 `.env` 中设置的角色提示词。
- 命令结果统一包含 `Command`、`Working directory`、`Exit code` 和 `Output`，便于模型准确汇总执行情况。
- 最终回答必须说明尝试了哪些检查、退出码和关键结果；用户拒绝审批时必须说明检查未运行。

### 本地 Git 保存

- 新增 `git_stage`，只接受最多 50 个明确的工作区相对文件路径。
- 拒绝 `.`、目录、工作区外路径、受保护目录、`.env` 和常见私钥文件，不提供 `git add -A` 或任意 pathspec。
- 暂存预览同时展示当前状态、已暂存内容、工作区 diff 和未跟踪文本文件内容。
- 暂存预览包含完整文件状态生成的 `Change-ID`；文件或暂存区变化后必须重新批准。
- `git add` 可能运行仓库配置的 clean filter，因此预览会明确提示风险，并始终要求人工批准。
- 新增 `git_commit`，只接受不超过 200 字符的单行提交消息，并在执行前展示暂存区 diff；超长内容会截断，但 `Change-ID` 覆盖完整 diff。
- 本地提交禁用 Git hooks 和 GPG 签名，不支持 amend、空提交、任意参数或推送。
- 暂存完成后提醒模型先调用 `git_diff(staged=true)`；提交完成后明确告知模型不能声称已经推送。
- CLI 当前注册 10 个工具，其中写入、质量检查、暂存和提交共 5 个工具必须人工确认。
- 增加主动验证提示、路径边界、敏感文件、旧审批失效、空暂存区和安全提交参数测试。

2026-07-19，Neil 已使用真实 DeepSeek API 验证修改后主动检查、结果汇总，以及 `git_stage` / `git_commit` 的批准和拒绝流程，确认本地版本保存闭环可用。

## 2026-07-19：可见任务计划和状态

### 任务计划

- 新增 `set_task_plan`，为多步骤开发任务创建 1–5 个简洁步骤。
- 新计划自动把第一步设为 `in_progress`，其余步骤设为 `pending`。
- 新增 `update_task_step`；只有当前进行中的步骤可以完成，完成后自动启动下一步。
- 计划必须按顺序推进，已完成步骤不能重新打开，并拒绝空白、重复或超过 200 字符的步骤。
- 计划只保存在当前 CLI 会话内；`/clear` 会同时清除对话、计划和最近检查。
- Agent 的内置工作流要求模型只为多步骤开发任务创建计划，并尽量把状态更新与相关工具调用合并。
- 计划创建或变化后由 CLI 立即展示，不需要等待最终回答。

### `/status`

- 新增 `/status`，不调用 DeepSeek，也不消耗 API 额度。
- 状态页展示当前计划及 `pending`、`in_progress`、`completed` 状态。
- 记录最近一次 `run_quality_check`，区分 `passed`、`failed` 和因拒绝审批导致的 `not_run`。
- 展示最近检查的命令、退出码和最多 500 字符的关键输出。
- 每次查看状态时通过只读 Git 命令获取最新分支和工作区状态，不依赖旧缓存。
- `/status` 的 Git 快照最多等待 5 秒，避免 Git 偶发卡顿阻塞 CLI。
- CLI 当前注册 12 个工具；两个计划工具只修改内存状态，不需要审批，高风险工具数量仍为 5 个。
- 增加计划限制、顺序推进、即时通知、检查状态解析、清理和 CLI 状态展示测试。

## 2026-07-19：详细实时执行轨迹

### 活动事件

- Agent 在每次模型请求前报告当前阶段、模型轮次、上下文消息数和可用工具数，并在模型返回后列出本轮选择的工具。
- 每个工具调用都会报告执行中、等待批准、完成、失败或跳过；完成和失败事件包含本次工具流程耗时。
- 文件工具显示路径、内容规模、预计替换数、目录条目、搜索范围、查询、匹配数和前几个结果位置。
- 命令与 Git 工具显示实际命令、退出码和有界结果摘要；Git diff 额外显示差异规模与涉及文件。
- 高风险工具把“生成预览”“等待用户批准”“批准后实际执行”分成明确阶段，审批拒绝使用独立的 `skipped` 状态。
- 活动轨迹只展示可观察的输入与结果，不展示模型思维链；文件正文和替换前后文本不会复制到活动详情。
- 所有自由文本详情都会移除终端控制字符、压缩为单行并限制长度，避免输出注入和终端刷屏。

### 终端渲染

- `TerminalRenderer` 使用 Rich 动态 spinner 和持续增长的耗时表示正在等待模型或工具，收到文本、计划或完成事件时自动停止。
- 持久状态使用 `[?]`、`[ok]`、`[-]`、`[!]`，分别表示等待批准、成功、跳过和失败，并在其下缩进展示结构化详情。
- 如果模型先输出文字再调用工具，渲染器会关闭当前回答行；工具结束后的文字会重新显示 `Neil Agent >` 前缀。
- 任务计划更新也会先安全结束当前回答片段，不再依赖散落在 CLI 中的手动换行。
- 增加模型与工具事件顺序、命令结果摘要、文件内容隐藏、审批阶段、动态状态生命周期、流式文本续接和计划插入测试；当前测试总数为 68。

2026-07-20，Neil 已使用真实 DeepSeek API 验证详细执行活动、任务计划的创建与自动推进，以及 `/status`，确认终端实时反馈闭环可用。

## 2026-07-20：本地会话保存和恢复

### 版本化快照

- 新增 `session.py`，使用带 `version: 1` 的严格 JSON 快照保存完整成功对话、任务计划和最近一次质量检查。
- 快照只接受预定义字段，不读取或保存 `.env`、系统提示词、API Key 和其他环境变量。
- 会话保存在工作区的 `.neil-agent/sessions/`；该目录加入 `.gitignore`，并被文件工具和 Git 暂存工具视为受保护目录。
- 单个会话文件最大 25 MB；读取时验证文件大小、会话 ID、文件名、版本、时区、完整消息轮次和任务步骤顺序。
- 文件必须位于真实会话目录中，拒绝路径穿越和符号链接绕过。
- 保存使用同目录临时文件、刷新与 `fsync`、`os.replace` 原子替换；失败时保留旧快照并清理临时文件。

### CLI 会话生命周期

- CLI 启动时创建新的内存会话 ID，但不会写入无内容的空会话。
- 每次完整回答成功后自动保存；失败或被中断的请求不会覆盖最后一次有效快照。
- 新增 `/sessions`，按更新时间列出最多 50 个有效会话、轮数和最近用户请求摘要，不调用 DeepSeek。
- 损坏或未来版本的会话不会阻塞列表，而是被跳过并显示数量。
- 新增 `/resume <session-id>`，只有用户明确指定精确 ID 时才恢复消息、计划和最近检查；启动时不会自动恢复。
- `/clear` 保留已经保存的旧会话，并创建新的会话 ID；继续对话时会保存到新文件。
- `/status` 现在同时显示当前会话 ID；本地保存失败只显示警告，不会丢弃已经生成的模型回答。
- 增加快照往返、环境秘密排除、原子失败、损坏文件、消息轮次、计划恢复、目录保护、CLI 列表/恢复和自动保存测试；当前测试总数为 76。

2026-07-21，Neil 已使用真实 DeepSeek API 验证退出进程后的 `/sessions`、`/resume`，以及恢复会话后继续调用工具，确认跨进程恢复闭环可用。

## 2026-07-21：上下文预算和本地会话管理

### 完整轮次上下文预算

- 新增 `context.py`，以发送给 API 的紧凑 JSON 字符数近似估算上下文；估算包含系统提示词、工具定义、消息文本、思考块、工具调用参数和工具结果。
- 新增 `MAX_CONTEXT_CHARS`，默认值为 `120000`；它与 `MAX_ROUNDS` 同时生效，前者限制近似请求规模，后者限制历史轮数。
- Agent 从最新历史向前选择连续的完整轮次；如果某轮不再适合剩余预算，就连同更早历史一起省略，不会拆散工具调用和对应结果，也不会跳过新轮而保留更旧轮次。
- 预算只裁剪以前已完成的历史。当前用户输入始终发送，正在执行的模型/工具循环也保持完整，因此极端情况下本次请求仍可能超过软预算并由模型 API 返回错误。
- 新增 `/context`，不调用 DeepSeek；它展示预算、系统提示词与工具定义的固定开销、已保存历史、下次请求可带历史以及被省略的轮数。
- `/context` 的“下次请求”统计尚未包含下一条未知的用户输入，因此实际发送历史可能进一步减少；字符估算用于本地保护，不等同于模型精确 token 计费。

### 显式会话清理

- `/sessions` 现在展示所有 JSON 会话文件的总存储占用、有效会话数，以及每个已列出会话的文件大小。
- 新增 `/delete-session <session-id>`：先读取并展示精确 ID、更新时间、轮数、文件大小和最近请求，再要求用户输入 `y` 或 `yes`。
- 空输入、其他回答、终端中断都视为取消；当前活动会话禁止删除，必须先用 `/clear` 或 `/resume` 切换。
- 删除前和实际删除时都会重新校验会话 ID、文件位置、格式和内容；损坏文件仍只计数和跳过，不提供自动清理。
- 程序不会根据预算、数量或存储大小自动删除历史会话，也不增加云同步。
- 增加上下文空状态、连续轮次选择、大型工具结果、实际请求裁剪、存储统计、删除确认/拒绝和当前会话保护测试；当前测试总数为 84。

2026-07-21，Neil 已使用真实 DeepSeek API 验证大型工具结果触发历史省略，以及 `/delete-session` 的批准、拒绝和当前会话保护，确认上下文与本地清理边界可用。

## 2026-07-21：模型请求可靠性和本地自检

### 可观察的有界重试

- Anthropic SDK 的内置重试被关闭，避免底层发生用户看不到的重复等待；重试次数和反馈统一由 `LLMClient` 管理。
- 新增 `MAX_RETRIES`、`RETRY_BASE_DELAY` 和 `RETRY_MAX_DELAY`，默认最多重试 2 次，等待从 1 秒指数增长且单次不超过 8 秒；总请求次数等于首次请求加重试次数。
- 只重试 DeepSeek 限流、HTTP 408、HTTP 5xx、请求超时和连接失败；鉴权、权限、请求格式及其他 4xx 错误立即返回，不浪费时间或额度。
- 对 `Retry-After` 和 `retry-after-ms` 响应头进行解析，但服务端建议等待仍受 `RETRY_MAX_DELAY` 约束，避免终端长时间无响应。
- 每次重试前通过现有活动渲染器显示固定的安全原因、当前次数和等待秒数；等待结束后切换为“重试模型请求”的动态状态。
- 流式请求只有在尚未向终端输出正文时才允许重试；一旦用户已经看到文本，后续错误直接报告，避免重复片段和难以判断的部分回答。
- 重试等待可被 `Ctrl+C` 中断，失败请求和取消请求仍不会写入会话历史。

### `/doctor` 本地诊断

- 新增 `diagnostics.py` 和 `/doctor`，检查已加载配置、工作区读写能力、本地会话目录和 Git 仓库状态，全程不调用 DeepSeek。
- 配置检查只显示“API Key 已配置（值已隐藏）”，不会读取到报告、终端详情或异常文本中；API 地址使用非 HTTPS 时给出警告。
- 会话检查复用严格的快照读取边界，报告有效文件数、总占用和损坏文件数，不自动修复或删除。
- Git 检查复用受限的只读状态命令，只报告仓库是否可访问及工作区是否干净，不展示完整文件列表。
- 诊断结果区分通过、警告和错误；Git 不可用只降低相关功能，不阻止基础聊天。
- 增加 SDK 重试关闭、连接恢复、指数等待、服务端等待上限、鉴权/格式错误、重试次数耗尽、流式重复保护、配置校验、密钥隐藏和诊断降级测试；当前测试总数为 95。

本阶段没有故意制造真实 DeepSeek 限流或网络故障；重试上限、反馈顺序和流式重复保护已通过可控 SDK 异常测试验证，真实外部异常仍保留为人工验收项。

## 2026-07-21：项目指令和长会话连续性

### 根目录 `AGENTS.md`

- 新增 `instructions.py`，启动时只检查 `WORKSPACE_ROOT/AGENTS.md`；不存在或空文件不会阻止启动。
- 文件固定上限为 32768 字节，只接受真实普通文件和 UTF-8 文本；拒绝符号链接、越过根目录的解析结果、无效编码、Unicode 控制/格式字符和读取过程中发生的路径变化。
- 有效内容经过换行规范化和首尾空白清理，再以明确边界追加到系统上下文；内置工具与压缩规则仍位于其后。
- 项目指令只存在于当前进程的系统上下文，不作为用户/助手消息，也不写入版本 1 会话快照；恢复会话时使用当前启动时读取的文件。
- 新增 `/instructions`，只显示状态、绝对来源、字节数、字符数和固定错误原因，不显示指令正文、系统提示词或其他隐藏配置。
- 启动页会提示指令已加载或文件无效；文件在进程运行期间发生变化不会自动热加载，需要重启后生效。

### 显式 `/compact`

- `/compact` 只在用户明确输入命令后运行，至少需要 3 个完整轮次；默认把较早轮次总结为一个有标记的压缩检查点，并原样保留最近 2 个完整轮次。
- 压缩输入使用旧消息的 API JSON 表示，保留文本、工具调用、参数和结果的结构；单个旧轮次最多送入 20000 字符，超出时保留前后内容并加入明确截断标记。
- 当旧历史无法放入一次 `MAX_CONTEXT_CHARS` 预算时，Agent 会按完整轮次分批生成滚动摘要；极端大轮次会进一步按当前预算有界缩减，单次 `/compact` 最多发起 8 次模型请求。
- 摘要要求保留目标、约束、决定、文件、工具结果、验证和未完成事项，最大接受 8000 字符；模型返回空摘要、超限摘要或候选历史未实际变小时拒绝替换。
- Agent 先生成不可变的候选压缩历史，不修改当前消息；CLI 先使用原子会话保存写入候选，成功后才应用到内存。模型失败、取消、过期结果或磁盘保存失败都会保持原历史。
- 压缩检查点在 `MAX_ROUNDS` 清理和下一次请求选择中被识别为固定轮次，在字符预算允许时优先保留；`/clear` 或下一次成功 `/compact` 才会替换它。
- 压缩过程复用模型重试活动，结束后显示总结轮数、完整保留轮数、模型请求次数和近似减少字符数；任务计划和最近质量检查保持不变。

### 回归场景

- `evals/tasks.json` 从空列表扩展为 5 个可执行人工场景，覆盖根指令遵循、不安全指令拒绝、压缩后连续性、压缩失败原子性，以及重试/审批/恢复一致性。
- 新增评测结构检查，以及指令编码、大小、控制字符、条件式符号链接、正文隐藏、系统上下文注入、会话排除、工具轮次保留、超大轮次缩减、固定检查点、过期结果和保存失败测试。
- 当前自动化结果为 111 项通过；1 项符号链接用例在当前 Windows 未提供创建权限时按平台条件跳过。

## 2026-07-21：分层项目指令、会话导航和可运行评测

### 分层项目指令生命周期

- 指令目标取启动目录；启动目录不在 `WORKSPACE_ROOT` 内时安全回退到根目录。加载器沿根目录到目标目录的真实祖先链查找 `AGENTS.md`，由外到内组合，更内层规则只作用于其目录和后代。
- 单文件仍限制 32768 字节，完整生效链新增 65536 字节累计上限；任一文件是符号链接、越界、编码无效、包含控制字符或使累计超限时，整条候选快照无效。
- `/instructions` 现在按顺序展示目标、生效来源、各文件大小和作用域，但继续隐藏正文与完整系统提示词。
- 新增 `/reload-instructions`；Agent 从基础提示词重新构建系统上下文，不清除历史。候选链完全有效后才替换，失败时继续使用旧快照。
- 新增 `/init`；它不调用模型，只读取有界的 `pyproject.toml`、`package.json`、`Cargo.toml` 或 `go.mod` 标记，生成短初稿和 unified diff。
- `/init` 只接受明确的 `y` / `yes`，批准后使用独占创建再次检查根 `AGENTS.md`，不会覆盖启动后或预览后出现的文件。

### 会话格式 v2 与本地搜索

- 当前快照写入版本 2 并新增标题；首次成功回答后从第一条用户请求确定性生成默认标题，不调用模型，也不把系统提示词写入快照。
- 旧版本 1 快照继续严格读取，在下一次保存或重命名时迁移为版本 2；未知未来版本继续作为不兼容文件跳过。
- 新增 `/rename-session <标题>`，修改当前会话标题；空标题、超过 80 个字符或包含控制/格式字符的标题会被拒绝。
- `/sessions [关键词]` 在本地按标题、会话 ID 和最近请求进行不区分大小写的包含搜索，同时显示全部有效数量与匹配数量，不调用模型。

### 可执行评测

- 新增 `neil-agent-eval` 命令，把 `evals/tasks.json` 的 5 个场景接入确定性执行器；默认使用假模型和临时工作区，不读取 API Key、不联网、不修改真实项目。
- 离线评测覆盖指令注入与会话排除、不安全指令拒绝、压缩连续性、压缩失败原子性，以及注入一次 SDK 连接异常后的真实重试分支、写入拒绝与会话恢复。
- 未注册执行器的场景会失败并返回非零退出码，避免评测文件退化为无人执行的文字清单。
- 真实模式必须同时提供 `--real-deepseek --confirm-api-cost`，只在临时工作区验证项目指令、`read_file`、压缩与新 Agent 恢复；自然重试只观察和报告，不主动制造外部故障。
- 当前自动化结果为 125 项通过；1 项符号链接用例在当前 Windows 未提供创建权限时按平台条件跳过。

## 2026-07-21：响应式启动工作台

- 原有模型、思考模式、工作区、会话和工具数量的逐行输出合并为一个 Rich 边框面板，在第一次输入提示前建立清晰的运行上下文。
- 面板使用 Neil Agent 自有的绿色视觉标识和“本地 Coding Agent 已准备就绪”状态，不复用 Claude Code 的名称、图形或品牌文案。
- 启动信息现在同时展示可用工具数、需要审批的工具数，以及项目指令的生效来源数量和总大小。
- 项目指令缺失时直接提示 `/init`，空文件提示 `/reload-instructions`，无效文件显示固定安全原因；任何状态都不展示指令正文、系统提示词或 API Key。
- 模型名、工作区路径和会话 ID 使用 Rich 纯文本节点，不解析运行时 markup；长路径和窄终端会自动折行。
- 面板底部提供 `/help`、`/doctor`、`/instructions` 三个起步入口，以及 `Ctrl+C` 和 `/exit` 提示；原有命令和对话行为不变。
- 新增真实 Rich Console 渲染测试，覆盖正常宽度、52 列窄终端、有效/缺失/无效指令状态和正文隐藏；当前自动化结果为 128 项通过，1 项 Windows 符号链接条件跳过。

## 2026-07-22：作用域指令、可移植会话与双重上下文预算

### 文件作用域指令

- 新增 `ProjectInstructionManager`，文件工具每次根据实际 `path` 解析目标目录，并沿工作区根目录到目标重新计算有效 `AGENTS.md` 链。
- 生效链变化时，Agent 先更新系统上下文并返回“本次文件操作未执行”的工具结果；模型在下一轮看到新规则后才能重新调用，避免首次访问越过内层规则。
- 作用域文件无效、目标越界或父目录不存在时直接拒绝文件操作；相同有效提示词仅更新内部目标，不额外消耗工具轮次。
- `/instructions` 和 `/reload-instructions` 跟随当前实际作用域，项目规则继续不写入会话快照。

### 可执行评测和本地质量检查

- `neil-agent-eval` 新增可重复的 `--task <id>`、`--format text|json` 和每场景毫秒耗时；未知或重复场景明确失败。
- JSON 报告包含固定的汇总与结果字段，便于脚本和 CI 消费；耗时使用可注入时钟测试，不依赖真实等待。
- `run_quality_check` 增加固定的 `eval` 枚举，执行 `python -m neil_agent.evals --format json`；它仍需要审批，且永不隐式添加真实 API 参数。

### 会话导航和安全交换

- `/sessions` 新增 `--page`、`--page-size`、`--sort updated|title`、`--order asc|desc` 和 `--state all|planned|failed|compacted`，所有搜索、排序和筛选都在本地完成。
- 会话摘要增加有计划、检查失败、含压缩检查点三个状态标记；分页结果同时保留有效数、匹配数和总占用统计。
- `/export [id]` 预览后把严格版本化信封独占创建到 `.neil-agent/exports/`；信封只包含版本 2 会话，不包含 API Key、环境变量、系统提示词或项目指令。
- `/import <文件名>` 只接受导出目录中的真实普通文件，验证文件名、25 MB 上限、版本、完整消息、重复 ID 和工作区边界；批准后再次校验 SHA-256 与目标不存在，拒绝预览后变化和覆盖。

### Token 预算适配

- 新增可选 `MAX_CONTEXT_TOKENS`；不配置时原有 `MAX_CONTEXT_CHARS` 行为完全保留，两者配置时必须同时满足。
- 当前模型无关估算对 ASCII 使用约 4 字符/token，对非 ASCII 使用约 1 字符/token；这是保守软预算，不声称等同 DeepSeek tokenizer 或计费数据。
- 普通历史选择、压缩请求和 `/context` 均接入 token 统计，同时继续按完整对话轮次裁剪。
- 自动化结果为 138 项通过；1 项 Windows 符号链接用例在当前环境无创建权限时跳过。真实 DeepSeek 验收未自动执行，仍需用户显式提供 `--confirm-api-cost`。

## 2026-07-22：Claude Code 官方文档对照审核

- 新增 `docs/claude-code-review.md`，按项目指令、权限、沙箱、会话、检查点、结构化输出与 hooks 对照官方行为，区分已对齐、明确差异和后续能力。
- 项目指令提示明确标为非可信仓库上下文，当前用户请求优先；文件、命令和 Git 权限仍只由代码控制。
- 新增 `/permissions`，展示直接与逐次审批工具、工作区、敏感路径、命令、网络和 OS 沙箱现状，不调用模型也不修改权限。
- 新增 `/branch [标题]`，完整复制消息、任务计划和最近检查到新会话 ID 后切换，原会话保持不变。
- `/compact [关注点]` 支持有界摘要关注点，并在覆盖当前快照前创建可恢复的“压缩前”分支；压缩不再是完整对话的唯一副本。
- 自动化结果为 140 项通过；1 项 Windows 符号链接用例按环境条件跳过。

## 2026-07-22：一次性协议、生命周期 hooks 与沙箱决策

### 一次性非交互运行

- 新增 `neil-agent -p/--print`，在不改变原交互入口的前提下执行一个 Agent 请求并退出；支持纯文本、单对象 JSON 和 JSONL `stream-json`。
- 一次性入口仅注册目录浏览、文件读取、文本搜索、Git 状态和 Git diff 五个只读工具，不注册写文件、质量检查、暂存或提交，也不存在隐式自动批准。
- 协议版本固定为 1；结构化输出不混入 Rich 装饰或思考内容，退出码明确区分成功 `0`、运行错误 `1`、参数/配置错误 `2` 和用户中断 `130`。
- 会话默认不落盘，只有显式 `--save-session` 才以独占创建保存成功结果；失败与中断不会产生快照。

### 类型化生命周期 hooks

- 新增 `hooks.py`，提供 `before_model`、`after_model`、`before_tool`、`after_tool` 四个进程内阶段；只接受宿主注册的 Python callable，不解析或运行任意 shell 字符串。
- 前置模型 hook 可拒绝或附加请求专用上下文，前置工具 hook 可在操作前拒绝；后置阶段仅审计，不能改变控制流或附加上下文。
- 单 hook 上下文、累计上下文、拒绝原因和每阶段回调数量都有上限，并拒绝不允许的控制/格式字符；回调异常和返回类型错误统一进入 `HookError` 边界。
- 工具前置失败采用 fail-closed，不执行 handler；工具后置审计失败会明确说明操作可能已经完成，不伪装成回滚。

### Token 与子进程安全结论

- DeepSeek 官方目前给出英文字符约 `0.3 token/字符`、中文字符约 `0.6 token/字符` 的近似值，并提供离线 tokenizer 演示包；没有为聊天前精确计数设计的轻量服务端接口。
- 本地软预算改用上述字符比例，其他非 ASCII 字符继续保守计算；实际 token 与费用仍以 DeepSeek 响应 `usage` 为准，文档不再把估算描述为精确 tokenizer 结果。
- 新增 `docs/sandbox-assessment.md`，评估 Windows AppContainer/Windows Sandbox 和 Linux bubblewrap。当前应用层白名单不声称是 OS 沙箱；在 fail-closed 文件与网络隔离后端完成前不开放通用 shell。
- 修正 `run_quality_check` 工具 schema 漏列离线 `eval` 的既有问题，使模型可见枚举和执行白名单重新一致。
- 自动化覆盖一次性三种输出、只读工具集合、错误清洗、显式保存、CLI 参数路由、hook 拒绝/审计/上限和 DeepSeek 估算比例；当前结果为 155 项通过、1 项 Windows 条件跳过。当前本地检查不调用真实 API；现有 `neil-agent-eval` 真实回归入口仍保留双重显式成本确认。

## 2026-07-23：服务端用量、协议契约、元数据审计与文件恢复

### DeepSeek 服务端用量与会话兼容

- 新增不可变 `TokenUsage`，接收输入、输出、缓存写入和缓存读取 token；同一 Agent 回合包含多轮工具调用时逐请求累加。
- `/context` 同时展示本地字符/token 软估算和最近一次成功 Agent 回合的服务端实测，明确历史实测不能预测下一次费用。
- 会话快照升级到版本 3 并保存可选 `last_usage`；版本 1、2 继续严格读取并迁移，重命名、分支、导出导入和显式一次性保存都会保留用量。
- 一次性 `json` / `stream-json` 成功结果包含 `usage`；服务端没有提供时稳定输出 `null`。

### 结构化协议契约

- 协议版本继续为 1；新增固定 JSON 夹具，精确约束单对象结果、JSONL 各事件、usage 字段和稳定错误代码。
- 结构化错误区分模型、Agent、工具、会话、指令、hook、审计、输入、配置、中断和内部错误；未知内部异常继续隐藏详情。
- 非交互入口仍只注册五个只读工具，没有审批输入协议，也没有隐式自动批准。

### 元数据专用本地审计

- 新增可选 `AUDIT_LOG_ENABLED` 与 `AUDIT_LOG_MAX_BYTES`；启用后在 `.neil-agent/audit/events.jsonl` 记录四个生命周期阶段。
- 审计只保留时间、阶段、轮次、消息/工具数量、工具名、结果规模和 usage 等有界元数据，不记录 prompt、thinking、工具参数值、工具结果正文或 API Key。
- 注册时预检目录、当前日志和备份必须是真实安全路径；单条限制 4096 字节，达到总量上限时只保留一个 `.1` 备份，并在写入后刷新磁盘。
- 审计失败进入明确的用户可见错误边界；一次性协议将底层审计写入失败稳定分类为 `audit_error`。

### 最小文件编辑检查点

- `write_file` / `replace_text` 原子成功后记录本进程内检查点，保存原内容、结果 SHA-256 和随机 ID；最多保留 20 项并限制累计原内容。
- 新增 `/rewind-file`，只处理最新一次 Agent 文件工具编辑，先展示反向 unified diff，再要求明确输入 `y` / `yes`。
- 恢复前后重新校验检查点、真实普通文件路径和当前内容哈希；文件缺失、符号链接替换、外部内容变化或批准后变化都会拒绝。
- Agent 新建文件的恢复语义是删除该文件；既有文件只恢复 UTF-8 正文。检查点不持久化，不恢复权限或目录，也不代替 Git 的跨进程、多文件可靠回退。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 23 个源文件检查通过。
- pytest 结果为 172 项通过、3 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、Ruff、mypy 与离线评测均不调用真实 DeepSeek API。
- 真实 API 验收仍保留双重显式成本确认；本轮不会自动消耗额度。

## 2026-07-23：基础只读可视化驾驶舱

- 新增 `/cockpit`，从当前内存状态生成一次性的 Rich 运行时快照，不调用模型、不写文件、不启动额外事件循环。
- 新增不可变 `CockpitSnapshot`，集中承载有界元数据；界面包含任务矩阵、基础上下文断层图、基础安全盾和工作区信号四个区域。
- 上下文区域区分字符/token 本地软预算与最近服务端实测 `usage`；任务区域显示计划步骤和最近质量检查。
- 安全区域如实区分直接执行与逐次审批工具、应用层工作区锁、元数据审计和当前未启用的 OS 沙箱，不夸大现有隔离能力。
- 工作区区域只显示项目指令状态/来源数/总字节数、进程内文件检查点数量和有界 Git 摘要；所有运行时字符串均按纯文本清洗和限制长度。
- 自动化覆盖正常宽度、52 列窄终端、运行时 Rich markup/控制字符防护，以及 CLI 路由、帮助入口和项目指令正文不泄露。
- 高级事件总线、实时 DAG、Context Tomography、Security Shield、Time Machine 和 Neural Map 不与基础界面混写，已拆分到 `docs/visualization-development.md`，可按独立检查清单间歇推进。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 24 个源文件检查通过。
- pytest 结果为 175 项通过、3 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试和离线评测未调用真实 DeepSeek API。

## 2026-07-23：非交互协议 v2 与一次性写入审批

### 双阶段显式审批

- 默认协议 v1 保持原有五个只读工具和精确字段契约；只有同时选择协议 v2 与 `request` / `approve` 权限模式才会注册受审批工具。
- `request` 运行允许模型生成高风险工具调用，但只持久化精确预览并向模型返回拒绝结果，不执行目标操作；终止事件为 `approval_required`，退出码为 `3`。
- `approve` 运行必须提供 request ID 和完全相同的 prompt；单次最多消费一个完全匹配的操作，后续高风险调用需要新的审批。
- v2 只支持结构化输出；成功、等待审批和错误对象均显式声明协议与权限模式，独立 `noninteractive_protocol_v2.json` 夹具固定字段集合。

### 预览绑定与重放保护

- 新增 `ApprovalStore` 与不可变 `ApprovalRequest`，在 `.neil-agent/approvals/` 保存 15 分钟有效的待审批记录和一次性消费标记；创建新请求时安全清理正常过期记录，待审批上限为 100 项。
- 记录绑定规范工作区、prompt 哈希、当前项目指令哈希、工具名、规范参数哈希和精确预览；prompt、项目指令与独立参数正文不落盘，必须展示给用户的 diff 预览除外。
- 调用方看到的 approval ID 同时包含完整审批记录摘要；外部进程即使能够修改 pending 文件，也不能把已展示授权替换为另一项操作。
- 审批目录和记录必须是真实工作区路径与普通文件，采用独占创建、`O_NOFOLLOW`（平台支持时）、文件描述符类型复核、大小上限和磁盘刷新。
- 匹配审批在工具 handler 之前原子消费；重放、过期、prompt 变化、作用域指令变化、参数变化和外部文件/Git 状态变化都会 fail-closed。
- 消费后注册表再次生成预览；若更窄窗口内发生变化，操作仍被拒绝，但旧授权不会恢复，避免失败重试意外扩大授权。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 25 个源文件检查通过。
- pytest 结果为 186 项通过、3 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约和离线评测均未调用真实 DeepSeek API。

## 2026-07-24：审计诊断与跨进程安全轮转

### 内核文件锁与失败语义

- 新增稳定的 `.neil-agent/audit/events.lock` 锁锚点；审计文件大小检查、单备份轮转和追加在同一个跨进程临界区完成，避免多个 Neil Agent 进程同时轮转或写入。
- Windows 使用 `msvcrt` 字节范围锁，POSIX 使用 `flock`；锁归属由内核维护，进程正常退出或崩溃都会释放，不依据 PID 或时间戳猜测、删除所谓陈旧锁。
- 默认最多等待 2 秒并按固定短间隔重试；超时、锁文件不安全或底层锁异常都转换为 `AuditError` 并 fail-closed，不会绕过审计继续执行。
- 锁、当前日志和备份继续要求真实普通文件；单条与单文件大小限制、`O_NOFOLLOW` 文件描述符复核和写入后磁盘刷新保持不变。

### `/doctor` 只读审计诊断

- `/doctor` 新增“生命周期审计”检查：未启用时明确报告可选关闭状态；启用时报告路径、当前/备份大小、轮转上限、有效记录数、无效记录数和锁状态。
- 诊断不会创建审计目录或文件；它先做非阻塞锁探测，锁空闲时在锁内扫描当前与备份 JSONL，锁繁忙时只给出警告而不与活动写入竞争。
- 无效记录只累计数量；报告和错误不回显日志行、prompt、thinking、工具参数、工具结果或凭据。
- 回归覆盖跨进程并发轮转、锁超时 fail-closed、锁路径符号链接、格式损坏诊断以及持锁进程崩溃后的内核自动释放。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 25 个源文件检查通过。
- pytest 结果为 193 项通过、4 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约和离线评测均未调用真实 DeepSeek API。

## 2026-07-24：高级可视化 Phase 0A 事件基础

### 版本化运行时事件

- 新增不可变 `RuntimeEvent` 与 `RuntimeMetadataItem`，固定版本 1，使用唯一事件 ID、稳定关联 ID、父事件 ID、时区时间、阶段、状态和有界元数据。
- Agent 回合、模型请求、工具调用、审批和质量检查各自分配稳定关联 ID；开始与结束事实使用独立事件 ID，子阶段关联父级开始事件，可供后续 DAG 和时间线投影。
- 元数据按阶段使用固定字段白名单，只允许工具名、计数、耗时、状态和服务端 token 用量等有界标量；未知字段、控制/格式字符、过长文本和负数计数直接拒绝。
- prompt、thinking、工具参数值、工具结果正文、审批预览和项目指令正文没有可写入字段；Agent 接入只记录长度、数量和状态。

### 有界只读旁路

- 新增进程内 `EventBus`，每个观察者拥有独立的固定容量队列和守护线程；Agent 发布只执行非阻塞入队，不等待观察者。
- 观察者异常被隔离并累计失败次数；单个队列已满时只丢该观察者的数据并累计丢弃次数，不影响其他观察者或 Agent 结果。
- 观察者数量、单队列容量和待投递数量均有明确边界；显式 `flush()` / `close()` 支持超时，但 Agent 主链路从不调用等待方法。
- 事件总线是可选注入能力；交互 CLI、非交互协议和基础 `/cockpit` 默认不创建它，因此当前输出契约和无观察者执行路径保持不变。
- 回归覆盖事件不可变性、ID/父子关系、字段白名单、控制字符、故障观察者、队列溢出、无观察者以及工具审批/质量检查关联，并验证敏感正文不会进入事件。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 26 个源文件检查通过。
- pytest 结果为 203 项通过、4 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约和离线评测均未调用真实 DeepSeek API。

## 2026-07-24：高级可视化 Phase 0B 事件存储与确定性投影

### 显式事件存储

- 新增 `JsonlEventStore`；构造保持无副作用，只有宿主显式注册到 `EventBus` 后才预检并使用 `.neil-agent/runtime-events/`。
- 版本 1 事件逐行写入 JSONL；单条和单文件大小均有上限，达到上限时只保留一个 `.1` 备份。锁、当前文件和备份必须是真实普通文件。
- 大小检查、轮转与追加位于同一个跨进程内核锁临界区，并在追加后刷新磁盘；符号链接替换、锁超时、截断记录、超限记录、未知版本或格式损坏都会拒绝该次存储操作并转换为 `EventStoreError`。
- 加载按备份到当前文件的追加顺序返回保留窗口，并重新通过严格 `RuntimeEvent` 模型校验；事件存储未接入默认 CLI，因此现有输出、磁盘写入和线程行为保持不变。
- 作为 `EventBus` 观察者运行时，写入异常遵循只读旁路语义：由总线隔离并计入观察者失败统计，不反向改变 Agent 执行结果。

### 确定性执行图与时间线

- 新增不可变 `ExecutionGraph`、`RuntimeTimeline` 及对应 projector；图和时间线完全由事件重建，不在 Agent 中维护第二份可变状态。
- 同一事件 ID 先按规范 JSON 去重；内容冲突时选择字典序最小的规范事件并记录异常。唯一事件再按 UTC 时间与事件 ID 排序，因此输入乱序和 metadata 字段顺序不改变结果。
- 缺失开始事件保留为孤立节点；多个开始、重复状态、父事件缺失/类型不符、终态父级不一致和结束早于开始均形成稳定异常。冲突终态使用 `failed > skipped > succeeded`；父级环通过移除环中字典序最大节点的父边确定性打断。
- 新增不依赖 Rich/Textual 的有界纯文本回放、固定 JSONL/文本夹具，以及覆盖重复、冲突、缺失、乱序和父级环的回归测试。10,000 个合成事件验证顺序稳定性，并约束耗时小于 10 秒、峰值内存小于 128 MiB。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 28 个源文件检查通过。
- pytest 结果为 218 项通过、5 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约和离线评测均未调用真实 DeepSeek API。

## 2026-07-24：高级可视化 Phase 1 树状实时 DAG

### Textual 全屏执行树

- 引入 `textual>=8.2.8,<9.0.0` 并更新 `uv.lock`；该版本声明支持 Python 3.9 以上，覆盖项目的 Python 3.13 基线。
- 新增 `/cockpit --live` 显式入口；界面底部可提交 Agent 请求，左侧按 Agent 回合 → 模型请求 → 工具/审批/检查展示实时执行树，右侧展示选中节点的安全遥测，底部保留有界回答流。
- 增加全部、进行中、失败和工具筛选，筛选时自动保留祖先上下文；新增 `MetricsProjector` 汇总节点状态、模型/工具数量、服务端 token 和已报告阶段耗时。
- 使用深色青紫视觉层级、语义状态色和响应式布局；宽度低于 88 列时隐藏详情抽屉并保留完整执行树、精简指标和输入区。120×40 与 60×24 均经过实际 SVG/PNG 渲染复核。

### 线程、安全与降级边界

- Agent 在 Textual thread worker 中执行；EventBus 观察者通过容量 1024 的合并桥调用线程安全 `post_message`，界面最多保留 10000 个事件，丢弃数量可见且不反向阻塞 Agent。
- 每批事件仍经过 Phase 0B 的确定性图投影，Textual 不维护第二套节点状态。详情只渲染状态、稳定 ID、时间、异常与白名单元数据；prompt、thinking、工具参数和结果正文不进入图。
- 高风险工具使用全屏 modal 展示原有精确预览，仅 `Y` 明确批准；取消或关闭会拒绝待处理审批。全屏期间经典 Rich 活动、重试和计划输出临时静默，退出后恢复。
- EventBus 只在实时模式期间挂载，退出后解除并关闭。非交互终端、Textual 导入失败或启动异常会返回原 `/cockpit` Rich 快照；现有交互模式与所有非交互协议保持不变。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 29 个源文件检查通过。
- pytest 结果为 228 项通过、5 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约、无头 Textual 测试和离线评测均未调用真实 DeepSeek API。

## 2026-07-26：任务级多文件检查点

### Agent 回合任务边界

- 一次用户请求对应的完整 `Agent.stream_chat()` 回合现在形成一个文件任务边界；同一回合的多个 `write_file` / `replace_text` 合并为一个检查点，交互终端、实时驾驶舱和非交互协议复用同一机制。
- 回合成功、失败或被取消都会结束边界；已经成功落盘的文件修改不会因后续模型/hook 失败而失去恢复记录。没有文件修改的回合不占历史。
- 同一路径在任务中反复修改只保留第一次写入前正文和最终结果哈希；最终回到任务前内容的净零修改会从活动检查点移除。
- 文件工具被宿主直接调用时继续兼容原行为，每次成功写入形成一个单文件任务检查点。

### 写前容量与有界预览

- 历史最多保留 20 个任务，单任务最多 50 个路径；任务前原正文和任务后回滚正文分别限制为 5,000,000 字符，单路径记录限制为 1,000 字符。
- 文件工具在原子写入前预检活动任务的路径数、原正文和结果正文容量；超限时 fail-closed，目标文件保持不变，并提示拆分任务或使用 Git。
- `/rewind-task` 取代单文件语义，`/rewind-file` 保留为兼容别名。恢复界面列出全部路径和操作，合并反向 diff 上限为 100,000 字符，并按文件公平分配预览预算，避免后续路径被完全隐藏。

### 恢复、回滚与威胁模型

- 准备阶段验证全部路径均为工作区内真实普通文件且哈希匹配；批准后在任何恢复写入前再次校验检查点 ID、完整路径范围、预览内容身份和所有文件哈希，每个文件修改前还有即时检查。
- 多文件恢复使用逐文件原子替换/删除；普通进程内步骤失败时，已应用路径按反向顺序写回批准时正文。回滚成功会保留检查点供重试；回滚不完整会明确报告可能不一致并要求使用 Git。
- 新增 `docs/checkpointing.md`，明确多文件恢复不具备进程/系统崩溃原子性，不持久化，不恢复目录、权限、ACL 或扩展属性，也无法消除不协作外部程序造成的全部竞态。
- 回归覆盖多文件分组、同文件重复编辑、失败回合收口、原/结果容量、文件数上限、全量预检、外部变化、符号链接和中途失败自动回滚。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 29 个源文件检查通过。
- pytest 结果为 239 项通过、5 项平台条件跳过；5 个内置离线评测场景全部通过。
- 自动化测试、协议契约、无头 Textual 测试和离线评测均未调用真实 DeepSeek API。

## 2026-07-26：真实协议验收与 Windows 沙箱能力门禁

### 成本受控的真实 DeepSeek 验收

- 扩展原有双重显式入口；仍必须同时提供 `--real-deepseek` 和
  `--confirm-api-cost`，单独提供任一参数都不会触发真实请求。
- 真实验收把所有文件、会话和审批固定在自动删除的临时工作区，并使用固定
  短系统提示词、关闭 thinking、1024 输出 token、40000 字符上下文、单轮
  工具上限、60 秒请求超时和最多一次瞬时失败重试，避免继承无界用户配置。
- v1 通过 `stream-json` 核对默认协议版本、只读标志和固定的五个只读工具；
  模型精确调用一次 `read_file`，最终回答同时满足项目指令和文件证据。
- v1 结果必须包含正值服务端 `usage`；显式保存的版本 3 会话必须以独占
  创建落盘，并保留完全一致的 `last_usage`。
- 压缩验收要求恰好一次真实摘要请求，核对摘要请求的服务端 `usage`，保存
  检查点后用新的离线 Agent 恢复，避免为纯恢复检查增加 API 调用。
- v2 request 只生成一项 `write_file` 精确预览且目标不存在；approve 使用
  同一 prompt 和 approval ID 精确执行；再次使用该 ID 会在模型调用前被
  `ApprovalStore` 拒绝，目标内容保持不变。
- 失败报告只使用固定阶段码，不输出原始模型文本、临时路径、审批预览或完整
  approval ID；v1 前置失败会停止后续真实阶段，避免无意义消耗。

### 平台无关沙箱契约

- 新增不可变 `SandboxLimits`、`SandboxPolicy`、`RunSpec`、
  `SandboxCapabilities` 和 `SandboxResult`，固定 `none` / 只读快照工作区、
  无网络、最小环境、绝对可执行文件、分离 argv、墙钟/输出/内存/进程上限，
  以及超时、取消、输出/资源超限和后端错误的终止语义。
- 环境变量从固定名称和值格式白名单构造，拒绝 Key、token、credential、
  password 和 proxy 类名称；脚本/批处理后缀、NUL、换行参数、相对 executable
  和越界工作目录在执行准备前拒绝。
- 快照必须是真实绝对目录且不能是卷根；遍历时拒绝路径任一 reparse
  component、符号链接/junction、硬链接、非普通文件、受保护目录和凭据
  文件，并限制 100000 个条目、50 MiB 单文件和 512 MiB 累计字节。
- 新增 `SandboxError`；后端不可用、能力不完整或策略无法完整强制时统一
  fail-closed，不存在普通 `subprocess` fallback。

### Windows Sandbox 适配与结构化诊断

- `WindowsSandboxBackend.probe()` 只读检查 Windows 平台和
  `WindowsSandbox.exe` / `wsb.exe`，不创建 profile、ACL、临时文件或进程。
- 配置生成器显式禁用 `VGpu`、networking、clipboard、printer 和 audio/video
  input，启用 Protected Client，只允许一个过滤快照的 read-only mapped
  folder；argv、环境值和 LogonCommand 不进入 XML。
- Windows Sandbox 会把不足的内存配置自动提高到至少 2048 MiB，因此适配层
  对无法兑现的更低限制直接拒绝，而不是把它错误描述为已强制。
- 新增 `SANDBOX_BACKEND=disabled|windows-sandbox`，默认关闭。`/doctor`
  在关闭时不 probe；显式启用后只有工作区、断网、取消、超时、输出、内存
  和进程树门禁全部 ready 才报告正常，其他状态均为错误，且不显示可执行
  路径或未经信任的探测正文。
- 本机没有 Windows Sandbox 可执行组件，且可信的无头结果通道与完整进程树
  取消尚未实现，因此后端状态不是 ready，`run()` 始终拒绝。现有固定质量
  检查与 Git 命令没有改走新后端，也没有注册通用命令。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 30 个源文件检查通过。
- pytest 最终结果为 285 项通过、6 项平台条件跳过；新增单元测试覆盖策略、
  argv/环境、敏感快照、符号链接/重解析点、硬链接、容量、Windows XML、
  disabled/unavailable/incomplete/ready 诊断，以及绝不启动宿主 fallback。
- 5 个内置离线评测场景全部通过，不读取真实 Key、不访问网络。
- 用户显式批准后运行真实 DeepSeek 验收，5/5 通过：v1 使用 1656 tokens，
  压缩使用 3819 tokens，v2 request/approve 合计 6510 tokens，总计 11985
  个服务端报告 tokens；观察到 0 次自然重试。
- 因目标平台能力门禁未通过，真实网络隔离、子进程树终止和内核级逃逸测试
  没有伪装成已执行；按照预定条件继续维持固定命令白名单。

## 2026-07-26：Windows Sandbox 候选执行链与强制安全任务

### 过滤快照与确定性 manifest

- 新增 `sandbox_snapshot.py`，把真实仓库复制到独占临时目录；敏感目录、
  凭据文件、`.env`、私钥、缓存和虚拟环境不进入快照，`.env.example`
  保留。遍历顺序、manifest JSON 与 SHA-256 digest 都是确定性的。
- 文件数、单文件和累计字节在复制前及流式复制过程中受限；每个文件记录
  相对路径、类型、字节数和内容 SHA-256。源文件 identity、size、mtime，
  以及目录 identity 和前后条目集合都必须稳定。
- POSIX 使用 `O_NOFOLLOW`；Windows 对每层目录和文件使用
  `OPEN_REPARSE_POINT`，目录额外使用 `BACKUP_SEMANTICS`，且整个遍历窗口
  不共享 delete，从内核层阻止 junction/rename 替换。符号链接、reparse
  point、硬链接、非普通文件及任何 Win32 句柄 open/query/close 异常都
  fail-closed。
- `PreparedSnapshot` 只清理创建时绑定 identity 的目录；清理逐项复核类型、
  identity 和 reparse 状态，发现并发替换就保守留下临时目录，不递归进入
  未知目标。命令对 guest 快照的修改不会回写真实仓库。

### 固定 guest runner 与 canonical 结果

- 新增 `sandbox_guest.py` 和固定 C# runner 源码。请求只接受 snapshot 内
  相对 `.exe`、分离 argv、相对 cwd、`NEIL_` 前缀环境和有界超时、输出、
  内存、进程数；canonical JSON 同时绑定 run ID、request hash 和 instance
  ID，拒绝未知字段、重复 key、非 canonical 编码和路径逃逸。
- runner 用 `CREATE_SUSPENDED` 创建子进程，STARTUPINFOEX handle list 只
  继承 NUL/stdout/stderr；先配置 `KILL_ON_JOB_CLOSE`、active process、
  process memory 和 job memory，再把进程分配到匿名 Job 并恢复。
- stdout/stderr 由并行 reader 共享同一字节预算，不能先无界捕获再截断。
  timeout、取消、输出洪泛和 runner 异常都会终止完整 Job；退出前通过
  `QueryInformationJobObject` 确认 `ActiveProcesses == 0`。只有带
  `job_terminated=true`、结果 hash 和三重身份绑定的 canonical 结果可导出。
- reader 收口后再次检查共享预算，堵住子进程写超限后立即以 0 退出、主循环
  先观察到退出而把截断输出误报成功的竞态；真实编译 runner 与 1025 字节
  快速洪泛子程序验证 1024 字节预算仍稳定返回 `output_limit`。
- runner 构建只查找两个固定 .NET Framework `csc.exe` 位置，使用固定源码、
  参数、reference、最小环境和一次性目录；记录源码/二进制 SHA-256。已在
  本机用真实 Framework 编译器完成 compile smoke，并验证未知 mode 返回 64。

### `wsb.exe` 两阶段主机编排

- 新增未注册的 `WsbHostExecutor` 候选层，只接受真实 `wsb.exe`、显式 UUID、
  固定 `execute` / `export` 命令、`shell=False`、最小环境和有界 CLI
  stdout/stderr/JSON；GUI `WindowsSandbox.exe` 和用户拼接命令均被拒绝。
- start 配置显式关闭 network、vGPU、clipboard、printer、audio/video，
  启用 Protected Client，只读映射 snapshot 和 control，不含 LogonCommand
  或可写目录。
- 官方 `wsb exec` 不提供进程 I/O，因此执行阶段只让 runner 在 guest 内部
  落盘。runner 以 0 退出并确认 Job 为空后，host 才把一次性空 export
  目录动态映射为可写，再运行固定 exporter；解析完成后无条件 stop 显式
  实例。start/execute/share/export/parse/stop 任一失败、超时、取消或绑定
  变化都会拒绝结果，绝不退化为宿主进程。
- `--raw` 响应除严格限制字段、实例 ID 和 exit code 外，还按阶段验证状态；
  例如 `stop + Running`、`start + Stopped` 等显式矛盾状态一律 fail-closed。
  未返回状态字段的真实 schema 兼容性仍留给目标平台校准，不能据此标记 ready。

### 强制平台门禁与当前结论

- 新增攻击型 C# probe 和真实平台测试，覆盖宿主 sentinel/凭据与敏感环境
  不可见、snapshot 不可写、带宿主正向连通对照的外部 IPv4/IPv6/DNS
  阻断、已输出 child/grandchild 就绪证据的超时进程树清理、host cancel 后
  执行按阶段校验的 stop、stdout/stderr 洪泛、单进程内存和进程数上限；
  真实 Windows junction 替换锁另有专门测试。guest loopback 不是宿主地址，
  聚合 Job 内存与 cancel 前 guest tree 就绪仍保留为目标平台审查项，不
  伪装成已覆盖。
- 新增 `.github/workflows/windows-sandbox-security.yml`，仅在受保护、专用
  `windows-sandbox-security` 自托管 Windows runner 上串行执行，并固定
  `SANDBOX_REQUIRED=1`。该模式下缺组件、不能创建 junction 或测试 skip
  都是失败，不能作为 ready 证据。
- workflow 只允许受保护的 `main` ref，触发范围覆盖锁文件、全部源码和测试；
  选定安全用例写入独立 JUnit 报告，pytest 成功后仍会拒绝任何 skip，避免
  自托管 runner 缺能力时假绿。
- 当前机器是 Windows Home，且没有 `wsb.exe`，所以真实 WSB 攻击用例只在
  普通模式按设计 skip；强制 workflow 尚未产生目标平台证据。固定 runner
  与不可信进程仍共享 guest 身份，也尚无目标平台独立安全审查记录，因此
  安全保证明确保持 `candidate-job-only-not-certified`。
- `WindowsSandboxBackend.run()`、工具注册表和审批面均未接入候选执行器，
  `/doctor` 不会报告 ready；`run_command` 没有新增，现有固定命令白名单
  保持不变。这是硬门槛的预期结果，不以单元测试代替真实隔离认证。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 33 个源文件检查通过。
- pytest 最终结果为 390 项通过、15 项平台条件跳过；其中新增沙箱定向结果
  为 105 项通过、9 项跳过。真实 C# runner 编译测试已执行，WSB 用例因当前
  平台缺组件跳过。
- 5 个内置离线评测场景全部通过；本阶段没有再次调用真实 DeepSeek API。

## 2026-07-30：沙箱证据链、审批语义绑定与安全复核

### 一次性审批与执行语义绑定

- 审批记录升级为版本 2。每项记录除工作区、prompt、项目指令、工具参数和
  预览外，还强制携带 `binding kind/version/SHA-256`；普通工具使用域分离的
  `generic-tool/v1` 绑定。
- approve 在读取和验证 pending 正文前先以 `O_EXCL` 写入 terminal claim。
  并发调用只有一个获胜；记录损坏、临时篡改后恢复、prompt/指令/工具不匹配、
  模型失败或进程中断都不能恢复同一 ID。版本 1 的遗留 pending 记录会安全
  失效，不阻塞创建新审批。terminal marker 有 1000 项上限并保留 24 小时，
  避免任意无效 ID 造成无界目录增长；pending 自身在 15 分钟后已不可执行。
- 新增未接入工具注册表的 `RunCommandApprovalBinding`，确定性绑定 executable、
  分离 argv、逻辑 cwd、snapshot manifest、runner 源码/二进制 hash、后端和
  guest 协议版本、断网/只读/空环境/丢弃修改策略及全部资源上限。
- guest 请求协议和固定 runner 升级为版本 2。请求同时携带 manifest、runner
  source 和 approval digest；host 必须从实际请求语义与 runner binary 重新
  计算审批 digest，不能只比较调用者提供的两个字符串。复用安全命令的 digest
  构造另一条命令会在启动 WSB 前被拒绝。

### 停止后取证与不可变输入复核

- `WsbHostExecutor` 在 start 前、start 后、late writable share 前、export 后
  以及 stop 后重新扫描 snapshot 并核对 canonical manifest；control 内的
  runner、request 和全部绑定也在对应边界复核。
- writable export 中的结果不再于 guest 存活时读取。host 必须先严格确认
  `stop`，随后用有界 `wsb list --raw` 轮询目标 UUID 已消失，最后才首次打开
  结果文件；stop/list/复核任一步失败都拒绝结果。
- start/exec/share/export/stop 的 raw JSON 现在必须显式包含成功值和唯一状态
  字段，不能用字段缺失表示成功。执行器提供 pre-validation raw observer，
  使失败响应也能进入独立证据通道。
- 现有复扫能发现持续篡改，但不能排除外部同权限进程在两次扫描之间临时替换
  路径再恢复的 ABA。跨 start/share/stop 生命周期的 no-write/no-delete 句柄
  租约仍是接入前硬门槛。

### 可重复证据、认证门禁与审查结论

- 新增严格的 evidence schema、CLI 和文档。强制任务必须在同一受保护 revision
  上串行运行三轮，保存平台指纹、源码/锁文件/workflow、compiler/reference、
  runner/probe 源码与二进制 hash、固定攻击测试 JUnit 和每个 `--raw` 响应。
  workflow 为每次尝试创建随机化独占证据根和虚拟环境，只安装当前 revision
  构建的唯一 wheel；isolated Python 与 import-origin 检查阻止工作树源码覆盖
  被测产物。
- raw recorder 独占创建有界 JSONL，并把真实响应 bytes/SHA-256 与 repeat、
  nonce、完整固定 argv、instance/run/request 身份、返回码和终止状态一起写入
  canonical transcript；schema 只能从该 transcript 推导。每个 execution
  必须满足完整成功状态机或唯一允许的 host-cancel 状态机，stop 后最后一次
  list 必须证明目标实例消失。
- collect 强制 pytest 退出码为 0，并核对 JUnit tests/failures/errors/skipped
  汇总与 testcase 明细。三轮的 repeat ID、nonce、transcript 和证据 digest
  必须唯一，execution identity 集不能重叠；platform、subject、测试 manifest
  和规范化 CLI schema 必须完全相同。缺阶段、任何 skip/xfail/xpass/error/
  failure 或少于三轮都不能生成 aggregate。
- aggregate 不是认证。认证还要求独立 reviewer、零开放问题、review 与
  aggregate 精确绑定、显式 reviewer/digest trust pin 和有效期；默认空 trust
  配置必然拒绝。aggregate 绑定三轮起止时间，review 不得早于证据完成。
  workflow 只上传本次成功创建的动态证据根，并用固定 commit 的 upload action
  保存成功或失败的诊断 bundle；只有通过 verify 的 aggregate 才是有效证据。
- `SandboxCapabilities.ready` 不再接受裸布尔值，而由本地能力门禁和认证引用
  共同派生；只发现 GUI 不再被当作 CLI 候选，发现 `wsb.exe` 但无认证时报告
  `certification_required`。当前生产 probe 不注入认证。
- 独立代码审查确认审批摘要重算与 claim 前置问题已经修复，同时确认三个
  P0 尚未解决：runner 与不可信进程仍共享 guest SYSTEM 身份；WMI/SCM/
  Task Scheduler 等 broker 路径可能绕过 Job；snapshot/control 尚无执行全程
  句柄租约。普通 SHA-256 不是抵抗同身份写入者的认证机制，stop 后读取只能
  缩小竞态窗口，不能替代身份隔离。
- 本机为 Windows Home 且不存在 `wsb.exe`，无法生成目标平台证据。因此没有
  伪造 aggregate、独立 review 或 ready 状态；`WindowsSandboxBackend.run()`、
  `/doctor` ready 和 `run_command` 仍未接入，固定命令白名单保持不变。

### 验证说明

- Ruff lint 与全仓库格式检查通过；mypy 对 35 个源文件检查通过。
- pytest 最终结果为 513 项通过、15 项平台条件跳过；证据链与宿主状态机最终
  定向结果为 100 项通过、7 项跳过。真实 C# runner 编译测试已执行；WSB
  攻击用例因本机注册表产品为 Home 且缺少 `wsb.exe` 按设计跳过，未被计作认证。
- 最终 wheel 离线构建成功，并在全新虚拟环境中以 isolated import 从
  `site-packages` 加载；wheel-backed 证据单测 58 项通过。工作流 PowerShell
  脚本通过 AST 语法解析。
- 5 个内置离线评测场景全部通过；本阶段未调用真实 DeepSeek API，也未生成
  或伪造目标平台 aggregate、独立 review 或 ready 状态。

## 2026-07-31：实时驾驶舱自适应结果区

### 均衡布局与结果聚焦

- `/cockpit --live` 不再把回答区固定为 12 行、窄终端固定为 7 行；常规终端由
  DAG/遥测工作区和回答区等比分配剩余高度，回答区具有 14 行最小高度。
- 高度小于 36 行时启用独立的 `short` 密度：压缩品牌栏和指标栏、隐藏回答区
  标题，并按 `2:3` 把更多空间分给回答；宽度小于 88 列的详情隐藏规则保持
  独立，resize 可同时、可逆地切换两个维度。
- 新增可见的 `F2` 与兼容的 `Ctrl+O` 结果聚焦入口。展开时只隐藏 DAG/遥测，
  回答流、最近 500 行有界内容和底部输入保持原对象；再次触发无损恢复，并把
  焦点稳定留在输入框。回答区标题会明确显示当前动作和保留上限。
- 回答区改用完整圆角面板和聚焦青色边界。回答仍是当前交互视图中的纯文本，
  不写入 RuntimeEvent，不扩大事件或持久化敏感正文的边界；超长结果仍通过
  独立滚动查看。

### Modal 稳定性与验证

- 主驾驶舱组件查询现在固定落到 screen stack 的底层驾驶舱 Screen。工具审批
  Modal 打开期间，定时指标和事件刷新不再错误查询 Modal 并偶发触发
  `NoMatches`；审批的阻塞、取消和显式 `Y` 决策语义不变。
- 无头 Textual 回归覆盖 120×40 均衡布局、120×24 短终端、60×24 宽高组合、
  36 行边界、resize 往返、`F2`/`Ctrl+O` 展开恢复、内容与输入焦点保留，以及
  Modal 存续期间的定时刷新。
- 160×48 常规态/结果展开态和 80×28 窄矮态均生成真实 Textual SVG 并栅格化
  复核；面板比例、标题、边框、输入区和 Footer 无重叠或溢出。
- Ruff lint 通过，全仓库 68 个文件格式检查通过；mypy 对 35 个源文件检查
  通过；pytest 为 515 项通过、15 项平台条件跳过；5 个内置离线评测全部通过。
  所有检查均未调用真实 DeepSeek API。

## 2026-07-31：Phase 2A 上下文断层图第一批（1/3）

### 五层确定性本地估算

- 新增版本 1 的不可变 `ContextTomography` 与固定层序：系统固定开销、工具
  schema、项目指令、已选择历史和当前链路。快照只包含字符数、近似 token 数、
  项目数和轮次计数，不持有 prompt、消息、项目规则、工具参数或结果正文。
- 系统、项目和工具开销通过同一紧凑 API JSON 估算器做差分拆解；五层字符与
  token 总数严格守恒于完整固定开销、入选历史和当前链路之和。Agent 抽取共享
  历史选择路径，`context_stats()`、首轮模型请求和断层图使用相同的完整轮次
  裁剪规则，没有改变无可视化观察者时的请求内容。
- 本批的 `CURRENT CHAIN` 只计算提交时已规范化的用户消息，并明确显示
  `SUBMIT SNAPSHOT`。工具循环追加的 tool call/result 与 `before_model` hook
  动态上下文暂不计入，避免把不完整数据包装成实时全量请求。

### 同槽切换与响应式界面

- `/cockpit --live` 使用一个 `ContentSwitcher` 复用原监控工作区；`F3`（隐藏
  别名 `Ctrl+T`）在执行 DAG 与 Context 间切换，不新增会压缩回答区的纵向区域。
  DAG 的筛选/选择状态、回答内容和输入焦点均保留；Context 模式隐藏无关的
  `1`–`4` 筛选绑定。
- 宽屏以 `2:1` 显示五层占用与预算详情；宽度小于 88 列或高度小于 36 行时
  隐藏次要详情，五层主摘要仍完整可见。结果展开期间可以切换监控状态，按
  `F2` 恢复后进入最后选择的视图。
- 快照仅在界面挂载、用户提交和回合结束时更新，不在 250 ms 指标定时器中
  重复扫描历史。界面固定标记 `LOCAL ESTIMATE` 和“只显示计数”，本批没有
  混入历史服务端 usage、预算预警或模拟结果。

### 验证与后续边界

- 数据测试覆盖五层顺序、schema 版本、字符/token 双守恒、轮次数不变量、
  空工具/项目/token 上限、系统提示词尾随空白规范化，以及
  system/project/tool/history/current 五类 canary 不进入快照；Textual 测试
  覆盖 F3 往返、布局尺寸不变、窄矮终端、F2 组合、Modal 屏蔽、提交/结束
  快照刷新与输入焦点。
- 后续第二批补最近服务端实测的历史对照、压缩检查点和高占用工具结果等
  裁剪洞察；第三批补预算阈值预警与“不调用模型”的 what-if 模拟。完成这两批
  前，Phase 2A 保持“进行中（1/3）”。
- Ruff lint 与全仓库 68 个文件格式检查通过；mypy 对 35 个源文件检查通过；
  pytest 为 520 项通过、15 项平台条件跳过；5 个内置离线评测全部通过。
  160×48 宽屏与 80×28 窄矮屏均由真实 Textual 运行态导出并栅格化复核，
  后者无需滚动即可看到全部五层。本阶段未调用真实 DeepSeek API。

## 2026-08-01：Phase 2A 上下文断层图第二批（2/3）

### 服务端历史实测与确定性裁剪洞察

- `ContextTomography` 升级到版本 2，在五层本地估算之外保存历史总量、最近成功
  Agent 回合的服务端 `TokenUsage`、压缩检查点状态和有界工具结果占用洞察。
  本地下一次输入与服务端历史实测在模型和界面上保持独立；缓存创建、缓存读取、
  普通输入和输出 token 均按原始类别展示，并明确标为历史汇总而非预测。
- 裁剪状态同时报告被省略的完整轮次数、字符数和估算 token 数；压缩检查点使用
  `none`、`kept`、`omitted` 三态，避免仅凭轮次差推断压缩是否仍在请求中。
  这些数据全部复用实际历史选择结果，没有改变最近连续轮次优先的预算语义。
- 工具结果洞察只保留按 API JSON 估算的前三大占用、时间序号和入选/省略状态。
  快照不保存工具名称、调用 ID 或结果正文；测试使用 canary 验证敏感结果和 ID
  不会出现在快照表示中。入选结果还必须按对象身份来自保存历史，否则构建失败，
  防止错误地把省略项标成已保留。

### 响应式信息层级

- 宽屏 Context 视图左侧同时展示五层、历史裁剪、检查点和最大工具结果，右侧将
  `LOCAL NEXT INPUT · ESTIMATE` 与 `LAST SERVER MEASUREMENT` 分区，并固定显示
  `HISTORICAL · NOT A FORECAST`。工具占用区固定提示 `BODY HIDDEN`。
- 窄屏将第二批洞察压缩为裁剪/检查点、最大工具结果、服务端历史三行；矮屏继续
  完整保留五层，把同类信号收进边框副标题，不挤占回答流。160×48、80×28 和
  60×40 三种真实 Textual 运行态均已导出、栅格化并目视复核。
- 当前链路仍是提交时快照，不声称覆盖工具循环和 hook 的动态上下文。预算阈值
  告警与不调用模型的 what-if 模拟明确留给第三批，因此 Phase 2A 状态保持
  “进行中（2/3）”。

### 验证

- 数据测试覆盖 schema 版本、历史体积守恒、服务端类别、工具结果排序与状态、
  压缩检查点保留/省略、非法数量和非规范排序；Textual 测试覆盖宽屏分区、窄屏
  三行洞察、矮屏副标题以及五层无需滚动的布局。
- Ruff lint 与全仓库 68 个文件格式检查通过；mypy 对 35 个源文件检查通过；
  pytest 为 522 项通过、15 项平台条件跳过；5 个内置离线评测全部通过。
  全部验证均未调用真实 DeepSeek API。

## 2026-08-01：Phase 2A 上下文断层图第三批（3/3，已完成）

### 确定性预算压力

- 新增不可变 `ContextBudgetPressure`，只从五层本地估算和已配置软预算派生。
  字符与可选 token 预算均使用整数基点计算，选择压力更高的一项作为限制维度；
  低于 75%、75%–不足 90%、90%–100%、超过 100% 分别映射为
  `safe`、`warning`、`critical`、`exceeded`，避免浮点边界漂移。
- 界面使用 `SAFE`、`WATCH`、`HIGH`、`OVER` 文本、色带和面板边框共同表达
  压力。基础快照和模拟快照保持分离：基础压力不会被预测值覆盖，而模拟超过预算
  时仍会把 Context 面板边框提升为风险色。

### 完全本地的 what-if 模拟

- 新增版本 1 的 `ContextWhatIf` 与 `Agent.context_what_if()`；输入
  1–1,000,000 个字符后，以明确标注的 ASCII 约 0.3 token/字符假设构造临时
  下一次输入，并复用真实固定开销、完整轮次选择、字符/token 双预算与压缩检查点
  路径。结果报告投影总量、限制维度、压力等级和新增裁剪轮次。
- 模拟只返回计数，不保留合成输入；不会调用模型、工具或 lifecycle hook，不发布
  RuntimeEvent，不修改消息历史、最近服务端 usage 或任务状态。边界测试验证历史和
  模型请求列表在模拟前后完全一致。
- Context 视图新增 `F4` 模态输入；`0` 或“清除”按钮移除模拟，`Esc` 取消。
  执行 DAG 视图和正在运行的 Agent 回合会禁用该动作，避免无意义入口与并发读取。
  用户真正提交请求时旧模拟自动失效。

### 响应式呈现与阶段收口

- 宽屏右侧在同一视口内展示基础压力、字符/token 预算、what-if 前后对照、历史
  去留和完整服务端历史实测；60×40 窄屏压缩为五层加四行洞察；80×28 矮屏将
  基础/模拟压力和新增裁剪收进边框副标题。F4 对话框也在 80×28 下完成复核。
- 160×48、60×40、80×28 及 80×28 模态四种真实 Textual 运行态均已导出、
  栅格化并目视检查，内容、输入焦点、Footer 和回答区无重叠或非预期滚动。
- Phase 2A 三批至此全部完成；提交时快照仍明确不声称覆盖工具循环或 hook 动态
  上下文。下一可视化阶段为 Phase 2B Security Shield。

### 验证

- 数据测试覆盖四级压力边界、token 优先限制维度、what-if 上下界、完整轮次裁剪、
  不调用模型和不修改历史；Textual 测试覆盖 F4 可用域、模态输入、风险投影、清除
  与焦点恢复。Ruff lint 与全仓库 68 个文件格式检查通过；mypy 对 35 个源文件
  检查通过；pytest 为 530 项通过、15 项平台条件跳过；5 个内置离线评测全部
  通过。所有验证均未调用真实 DeepSeek API。

## 2026-08-02：Phase 2B Security Shield 第一批（1/3）

### 确定性能力模型与分层语义

- 新增版本 1 的不可变 `SecurityShield`、`SecurityCapability` 和
  `SecurityBoundary`。纯投影只接受工具名到“是否逐次审批”的布尔映射、审计开关
  及已观测 OS 沙箱能力；输入顺序不会改变结果，未知工具只进入聚合计数，工具名不
  保存在快照中。
- 当前 12 个应用工具稳定投影为 7 个直接执行和 5 个逐次审批工具，并按工作区
  读取、任务控制、Git 检查、工作区写入、质量检查和 Git 变更归组。任意宿主
  shell 固定显示为 `FORBIDDEN`，尚未暴露的 OS 命令能力显示为 `UNAVAILABLE`，
  四态同时具有文本、标记和颜色，不依赖颜色单独传意。
- 应用层 `ALLOWLIST ENFORCED` 与独立的 `OS SANDBOX` 状态使用不同数据结构和
  面板。`SANDBOX_BACKEND=disabled` 明确显示 `DISABLED BY CONFIG`；启用后只做
  现有 fail-closed 探测，探测失败、能力不完整或认证缺失都不会降级到宿主进程，
  也不会把路径校验、固定命令和审批误称为 OS 隔离。
- 快照不保存 prompt、thinking、工具参数、审批预览、工具结果、路径或秘密值；
  OS 探测返回的自由文本 summary 和可执行文件路径也不进入投影。

### 同槽 Security Shield 与响应式呈现

- `/cockpit --live` 新增 `F5` Security Shield，继续复用 DAG/Context 的监控槽，
  不新增纵向面板。再次按 `F5` 返回此前的主监控视图；`F3` 的 DAG/Context 语义、
  `F2` 结果聚焦、回答内容和输入焦点保持不变。
- 160×48 宽屏左侧一次展示全部八条能力带，右侧展示应用策略、OS 沙箱、审计信号
  和 `APPLICATION ALLOWLIST ≠ OS ISOLATION` 提醒；60×40 窄屏隐藏次要详情但
  保留八条色带；80×28 矮屏把永久禁止和不可用合并为一条有双标签的紧凑行，四态
  无需滚动即可识别，回答区与输入框仍可见。
- Rich `/cockpit` 基础快照和 `/permissions` 也消费相同投影：前者显示四态计数及
  两层摘要，后者不再使用“OS 沙箱当前未实现”这一含混静态文案。

### 验证与后续边界

- 新增投影测试覆盖 schema 版本、四态数量、输入顺序稳定性、未知工具聚合、禁用
  后端不探测、探测失败 fail-closed、自由文本 canary 不进入快照及矛盾元数据拒绝；
  Textual 测试覆盖格式器、F5 往返、动作可用域、焦点和窄矮布局。
- Ruff lint 通过，全仓库 70 个文件格式检查通过；mypy 对 36 个源文件检查通过；
  pytest 为 539 项通过、15 项平台条件跳过；5 个内置离线评测全部通过。全部验证
  未调用真实 DeepSeek API，也未改变工具权限、审批决策或沙箱执行行为。
- 下一批把审批请求、用户决策和“批准预览是否仍与执行绑定”关联到对应 DAG 节点；
  路径、网络、命令与审计边界的状态变化及有界告警留到第三批。

## 2026-08-02：Phase 2B Security Shield 第二批（2/3）

### 审批事实与预览绑定

- `RuntimeEvent` 增加严格枚举的可选 `approval_decision` 与 `preview_binding` 元数据；审批等待、批准、拒绝、不可用和错误均记录为既有执行事实，不写入工具参数、预览或结果正文。
- 工具注册表新增结构化的批准后执行结果。用户批准后仍重新生成当前预览并逐字比较：一致时标记 `valid` 并执行，不一致时标记 `changed` 并沿用原有拒绝结果，不可重新预览时标记 `unavailable`。旧的 `execute()` 接口和调用行为保持兼容。
- 审批节点在用户作出决定时立即结束，因此其耗时仍准确表示等待决策的时间；随后得到的最终预览绑定状态写入父工具终态。拒绝、缺少处理器和异常路径也会在父工具节点保留对应决策与绑定事实。

### 确定性关联与可视化

- 新增版本 1 的不可变 `ApprovalFlow` / `ApprovalTrace` 纯投影，按审批节点的父关联确定性连接工具节点，并从审批终态与父工具终态合成决策、最终绑定、预览字符数和等待耗时。事件输入反转或打乱不改变结果；旧版审批事件安全降级为 `NOT CHECKED`，不会把缺失事实推断为有效绑定。
- DAG 把审批作为工具子节点显示，在节点标签中直接标记 `APPROVED/CHANGED` 等状态；节点遥测优先展示 Decision、Preview Binding、Tool Node 和仅计数的 Preview Size，再显示完整 ID 与时间，关键安全结论首屏可见。
- Security Shield 新增最近审批流，宽屏显示关联工具短 ID 与耗时，60×40 窄屏保留最新完整决策行，80×28 矮屏把最近决策/绑定压缩进边框副标题。所有形态固定提示预览正文隐藏，且不新增会挤压回答流的纵向面板。

### 验证

- 单元和集成测试覆盖枚举校验、批准后有效/变化预览、拒绝与异常事实、乱序投影、旧事件兼容、工具筛选、DAG 节点详情、Security Shield 宽窄矮响应式呈现，以及绑定变化时工具处理器绝不执行。
- 160×48 DAG、160×48 Security Shield、60×40 窄屏和 80×28 矮屏均由真实 Textual 运行态导出并逐图检查；审批结论首屏可见，回答区、输入框和 Footer 无重叠或非预期裁切。
- Ruff lint 通过，70 个文件格式检查通过，mypy 对 36 个源文件检查通过，pytest 为 546 项通过、15 项平台条件跳过；5 个内置离线评测全部通过。全部验证均未调用真实 DeepSeek API，也未改变工具权限、审批决策或沙箱执行行为。
- Phase 2B 当前为“进行中（2/3）”；第三批继续补路径、网络、命令和审计边界的状态变化及有界告警。

## 2026-08-06：Phase 2B Security Shield 第三批（3/3，已完成）

### 四类边界与确定性变化投影

- `SecurityShield` 升级为 schema 2，除配置开关外只读观察审计记录器健康，明确区分 `recording`、锁占用 `busy`、`disabled`、记录损坏 `degraded` 与探测 `unavailable`；审计路径、记录内容和异常正文均不进入投影。
- 新增 schema 1 的不可变 `SecurityBoundaryWatch`，按固定顺序投影路径、网络、命令和审计四类无值信号。相邻 `SecurityShield` 快照生成确定性变化，内存观察窗口最多 64 份、变化最多 16 条、按稳定代码聚合的告警最多 8 个；截断数量显式可见。
- 路径和命令仍如实标为应用层约束，网络标为 Agent 工具面缺席；只有实际暴露 OS 命令工具且沙箱探测就绪时才允许标记 OS enforcement。OS 关闭或 fail-closed 绝不被包装成应用层已获得 OS 隔离。
- 告警覆盖 OS 隔离关闭/探测失败、审计锁占用/关闭/损坏/不可用、边界降级和观察器失败。观察失败保留上一份安全快照并只增加聚合故障计数，不中断 Agent，也不保留异常消息。

### 实时安全视图与响应式收口

- `/cockpit --live` 每次重新进入 `F5` Security Shield 时执行一次只读重采样；宽屏右侧按“分层事实 → 当前四边界 → 最近变化 → 有界告警”呈现，标题和边框同步反馈告警等级。基础 `/cockpit` 也显示同一单次纯投影与审计健康。
- 60×40 窄屏用 `P/N/C/A` 单行边界摘要保留四类状态、变化/告警计数，并在下一行完整保留最新审批工具与 `APPROVED/CHANGED` 等结论；80×28 矮屏把同样事实压入安全面板副标题，不增加会挤压回答区的固定高度。
- 160×48、60×40 和 80×28 三种真实 Textual 运行态均导出并逐图检查：宽屏变化与三个告警无需滚动即可完整读取，窄屏边界和审批同时可见，回答区、输入框与 Footer 无重叠或非预期裁切。

### 安全边界与验证

- 新投影只消费既有工具注册、沙箱能力与审计健康元数据；未新增工具、权限、网络能力、审批绕过、模型请求或持久化安全观察日志。告警文案固定且有界，不显示 prompt、工具参数/结果、路径、命令目标、审计内容、异常正文或秘密值。
- 单元和集成测试覆盖四类固定信号、相邻变化顺序、历史/告警上限、严重度优先保留、审计 busy/degraded/unavailable、观察失败保留上一快照，以及宽窄矮响应式信息保真。
- Ruff lint 与全仓库 70 个文件格式检查通过，mypy 对 36 个源文件检查通过；相关 64 项测试通过，全量 pytest 为 555 项通过、15 项平台条件跳过，5 个内置离线评测全部通过。全部验证未调用真实 DeepSeek API。
- Phase 2B 三批全部完成并正式收口；下一可视化阶段为 Phase 3A Time Machine 只读回放，先浏览事件和历史投影，不重新调用模型或工具，也不与安全恢复混合。

## 2026-08-08：Windows Sandbox 权限分层与执行期句柄租约

### restricted/Low Integrity guest 权限边界

- 固定 runner 升级为版本 3，安全保证改为
  `candidate-restricted-low-integrity-job-not-certified`；canonical guest 协议仍为
  版本 2。approval binding 自动绑定新的 runner 版本与二进制 hash，旧批准不能
  重放到新边界。
- 可信 runner 保持原 guest 身份，result 目录改为只继承 runner SID 的 DACL 和
  Medium Integrity `NO_WRITE_UP`；scratch 使用相同 runner DACL 和 Low Integrity。
  不可信命令不再继承 runner token，而是使用
  `DISABLE_MAX_PRIVILEGE | SANDBOX_INERT | LUA_TOKEN` 的一次性 restricted primary
  token，再降到 Low Integrity，并通过 `CreateProcessAsUser` 以 suspended 状态
  创建。原有 STARTUPINFOEX 三句柄 allowlist、Job 分配后恢复、输出/时间/资源上限
  与完整树终止语义保持不变。
- runner 只向子进程增加固定的自身 PID 与 scratch 路径元数据，不继承宿主环境。
  固定安全 probe 新增 token/完整性自检、scratch 正向写入、control/result 伪造、
  对 runner 的 `OpenProcess` 写/注入权限、SCM create-service、Task Scheduler、
  WMI `Win32_Process.Create` 和 `CREATE_BREAKAWAY_FROM_JOB` 攻击路径。

### snapshot/control/export 生命周期租约

- 新增 `sandbox_lease.py`。Windows 在 start 前为 snapshot/control 的根、全部目录与
  文件持有不共享 write/delete 的有界句柄；每次复核同时查询持有句柄和当前路径，
  严格比较 volume/file index、类型、链接数、大小和修改身份。reparse point、
  hardlink、非普通文件、条目/字节超限、路径集合变化及 open/query/read/close
  任一步异常均 fail-closed。
- export 根先用允许新增文件但不共享 delete 的句柄跨越 share/export；exporter
  返回后必须恰好出现唯一 `result.json`，host 立即取得只读封存树租约。该文件的
  写入、删除和重命名被阻止；结果直到严格 stop 且 `wsb list --raw` 确认 UUID
  消失后，才从已经持有的同一文件句柄读取。stop 报错不会跳过独立 list 确认，
  任一租约关闭失败会推翻原本有效的结果。
- 回归测试新增真实 Windows share-mode 写/delete/rename 阻断、新条目检测、export
  根替换阻断、租约上下限和清理失败；真实 C# runner 测试会等待子/孙进程写出
  `tree-ready` 后才触发取消，并验证完整 Job 已清空。目标 WSB 攻击集还区分单进程
  内存与聚合 Job 内存，覆盖 breakaway、受限令牌和三类 broker 逃逸。

### 证据门禁与当前状态

- 强制证据 manifest 从 8 项扩展到 14 项，并把新租约源码/测试、tree-ready 取消、
  token/broker/breakaway/聚合内存用例纳入 subject 与三轮 wheel-backed workflow。
- 当前开发机仍缺少可用 `wsb.exe`，真实 WSB 攻击测试按设计跳过；本阶段没有生成
  aggregate、独立 review、certification 或 ready 状态，也没有接入 backend、
  `/doctor` 或 `run_command`。

### 验证

- Ruff lint、全仓库格式检查和 mypy 对 37 个源文件检查通过。
- pytest 全量为 561 项通过、19 项平台条件跳过；真实 runner 编译、restricted
  token 启动、快速洪泛重分类、tree-ready 后取消与 Windows handle lease 回归
  已在本机执行通过。目标 WSB 用例的跳过不计为认证。
- 5 个内置离线评测全部通过。全部验证未调用真实 DeepSeek API，也未生成或伪造
  目标平台安全证据。

## 下一阶段计划

1. 在一次性、无敏感凭据的 Windows 11 Pro/Enterprise 24H2+ runner 上使用
   同一构建执行三轮强制 workflow，校准真实 raw schema，保存并校验完整 artifact
   bundle、Actions provenance/attestation 和自托管 runner 版本；定义 evidence
   到 review 的最大新鲜度，并由独立 reviewer 关闭固定 gate 集后 pin review
   digest。
2. 让认证 verifier 重新校验完整 raw bundle，只把未过期且与当前 commit、
   本机 OS/WSB/runner hash、协议和固定 gate 集完全一致的证据转换为不可伪造的
   运行时能力；随后才接入 backend 与 `/doctor`。
   以上门禁全部通过后再注册只接受 executable + argv、丢弃全部 guest 修改的
   最小 `run_command`；否则继续保持固定命令白名单。

## 2026-08-09：Windows Sandbox 证据重放、运行时认证与最小命令接入

### 完整证据与供应链 provenance

- 强制 workflow 继续在受保护 `main`、专用 Windows x64 environment 上串行执行
  三轮同构建测试；新增受保护的 `SANDBOX_ACTIONS_RUNNER_VERSION` 和
  `SANDBOX_ACTIONS_RUNNER_EPHEMERAL=1`，runner 版本与一次性声明进入 subject，
  缺失或非一次性配置直接失败。
- 使用固定 commit 的 `actions/attest` 为 replay-bound `aggregate.json` 生成
  SLSA/Sigstore provenance，并把本地 bundle 保存为
  `aggregate.attestation.sigstore.json`。上传前调用 GitHub CLI，严格校验固定
  repository、signer workflow、`main` ref 与 source commit；attestation 失败
  不能进入认证，但 `always()` artifact 仍保留失败现场。
- 新增完整 `bundle-verify`：不信任 summary，而是从三轮 `cli-raw.jsonl` 重建
  schema、从真实 JUnit 重建测试结果、重建每个 evidence run 和 aggregate；同时
  重新哈希唯一 wheel、runner 源码/二进制与 security probe。未知目录/文件、
  symlink/reparse point、hardlink、非 canonical JSON、raw/JUnit 篡改和 identity
  混用全部 fail-closed。
- 修正 Windows 对 `.exe` 的 path `stat` 会按后缀推导 execute bits、但
  `fstat(handle)` 不会推导造成的身份误判：稳定性比较改为比较内核文件类型，
  保留 device/inode/size/mtime 绑定。

### 独立审查、新鲜度与运行时能力

- 固定十四项 security gate 成为认证契约；approved review 必须关闭完整、精确的
  gate 集，reviewer 不能是 evidence producer，reviewer ID 与 review digest
  必须通过仓库外渠道 pin。新增 `review` CLI，生成 review 前也会完整重放 bundle
  和 Sigstore attestation。
- evidence 完成到独立 review 的最大间隔固定为 7 天；证书自身最长 90 天，并且
  绝不能晚于原始 evidence 完成后 90 天。`certify` 改为接收 bundle root，在签发
  前重新执行完整重放，不再只信任单独传入的 aggregate。
- 新增 `sandbox_runtime.py`，它是 production backend 获得 runtime
  certification 的唯一映射路径。每次执行前重新验证 raw bundle、attestation、
  pinned review、证书时效，并与当前 Git commit、受控源码 manifest、Windows
  build/edition、WSB feature/Authenticode/file hash、runner source/binary、C#
  compiler、framework reference、policy/protocol 和固定 gate 集逐项比较。
- runtime 配置要求同时提供 `SANDBOX_CERTIFICATION_ROOT`、
  `SANDBOX_TRUSTED_REVIEWER` 与 `SANDBOX_TRUSTED_REVIEW_SHA256`。缺失配置报告
  `certification_required`；存在但重放、签名或当前宿主绑定失败则报告
  `certification_invalid`，两者都不能变成 ready。

### backend、doctor 与最小 run_command

- `WindowsSandboxBackend` 已接入认证后的 `WsbHostExecutor`：为每次调用重新建立
  approval binding、guest request、只读 snapshot/control 与独立 transport，
  使用固定 runner 执行并映射有界 terminal result。任一准备、执行、stop/list、
  句柄租约或结果验证失败都拒绝结果，不存在宿主进程回退。
- `/doctor` 和 Security Shield 现在能区分缺少认证、认证无效与全部 gate ready；
  ready 仍由 certification 加强制能力组合推导，不能由调用方布尔值打开。
- 新增条件注册的 `run_command`。只有启动时 backend 已 ready 才进入工具表；
  非交互 read-only/v1 模式始终不注册。工具只接受工作区相对 `.exe` 与 argv，
  固定逻辑 cwd、禁网、空 guest 环境和过滤后的只读快照，逐次 approval preview
  绑定 snapshot/认证/runner/协议/资源上限，执行前再次比较 preview；认证轮换
  会令旧批准失效，所有 guest 修改统一丢弃。
- 当前开发机仍没有可用 `wsb.exe`，因此没有生成或伪造 aggregate、review、
  certification 或 ready 状态；目标 runner 实际运行与独立 review 是部署/发布
  操作门禁。没有这些真实材料时，本机继续只暴露原有固定命令白名单。

### 验证

- Ruff lint 与全仓库 75 个文件格式检查通过；mypy 对 39 个源文件检查通过。
- pytest 全量为 576 项通过、19 项目标平台条件跳过；新增完整 bundle 成功重放、
  raw 篡改拒绝、7 天 review/90 天 evidence 边界、精确 gate 集、runtime 缺省拒绝、
  backend 结果映射和 `run_command` 条件注册/丢弃修改回归。
- 安全 workflow 的两个 PowerShell run block 经 AST 解析无语法错误；5 个内置
  离线评测全部通过。全部验证未调用真实 DeepSeek API，目标 WSB skip 不计为
  认证。

## 下一阶段计划

1. 设计受控 guest 产物导出协议：只允许调用前声明的相对输出路径，生成有界、
   自哈希的 export manifest，拒绝 reparse point、hardlink、敏感文件、未知新增项
   和超限内容；首步只预览，不写回工作区。
2. 在现有文件检查点机制上实现二次批准的导入事务：把 certification、run/result、
   export manifest、逐文件 hash 与 bounded diff 全部纳入 approval binding，批准后
   再复核并原子应用；失败时完整回滚，不允许覆盖审批后变化的文件。
3. 将导入流程接入非交互 v2 request/approve、metadata-only audit 与离线评测，
   覆盖审批重放、证书轮换、工作区竞态、部分写入失败和恢复一致性；v1 与
   read-only 模式继续禁止导入。

## 2026-08-20：文档同步与 host_runtime 准备工作

### 文档

- 更新 `docs/architecture.md`：补充 Web Workbench 分层、`host_runtime.py`、五 Provider 配置项与 Phase 2B 状态。
- 重写 `docs/claude-code-review.md`：纳入 Web、多 Provider、沙箱认证与当前测试规模；列出 Web/CLI 已知差距。
- 修正 `docs/web-workbench-development.md`：`POST /api/v1/ws-ticket`、实际目录结构、已实现的事件类型。
- 新增 `docs/host-runtime.md`：三入口能力矩阵与迁移状态（后续条目已全部对齐，见该文档「迁移状态」表）。

### host_runtime 准备工作

- 新增 `src/neil_agent/host_runtime.py`：`HostMode`、`HostProfile`、`build_host_runtime()`、共享 `instruction_target()` 与 `windows_sandbox_backend()`。
- `cli.py`、`noninteractive.py`、`web/controller.py` 改为通过 `build_host_runtime()` 装配工具与指令上下文，消除三处重复代码。
- 新增 `tests/test_host_runtime.py`：指令目标、各 HostMode 工具矩阵与 Web 已知差距断言。

### 已知待办（未在本批实现；已于后续批次完成）

- ~~Web 会话 `SessionStore` load/save 与 `select_session` 命令。~~ → 2026-08-21 P8 / 2026-08-24 会话连续性。
- ~~共享 `SecurityShield` / cockpit DTO 映射供 Web 快照使用。~~ → 2026-08-21 host parity + 2026-08-25 Security 投影。

### 验证

- `tests/test_host_runtime.py` 与既有 Web/CLI 回归应全部通过。

## 2026-08-20：Web 与 CLI 沙箱及指令作用域对齐

### 运行时

- `HostMode.WEB` 现在与 CLI、非交互写入模式共用 `_register_sandbox_tools()`；认证未 ready 时仍不注册 `run_command`。
- 移除 Web 专用的 `workspace_root` 指令作用域；三入口统一使用 `instruction_target()`，即启动目录在工作区内时按子目录加载 `AGENTS.md` 链。

### 文档

- 更新 `docs/host-runtime.md` 能力矩阵与迁移状态。

### 验证

- `tests/test_host_runtime.py` 新增 Web cwd 作用域与沙箱注册路径回归。

## 2026-08-23：Phase 3A Time Machine 只读回放

### 历史投影与谱系数据

- 新增版本 1 的 `TimeMachineSnapshot` 与纯文本渲染器。投影复用既有时间线、
  DAG 和指标规范化逻辑，在最多 512 条事件上按稳定 event ID 选择游标并重建
  as-of 状态；输入乱序、重复和异常仍按既有确定性规则处理。
- 会话格式升级到版本 4，新增可选直接父会话 ID；版本 1–3 继续严格迁移，
  重命名、保存、导出导入和分支均保留谱系。`/branch` 只记录源会话为父节点，
  不继承审批或权限。
- 任务文件检查点新增带时区的 UTC 创建时间和不可变历史读取接口。Time Machine
  最多保留 50 个会话与 20 个进程内检查点，只投影根/分支/孤立分支、轮数、
  压缩/检查状态以及文件/字符计数。

### Textual 入口、持久化与隐私边界

- `/cockpit --live` 新增 `F6` Time Machine。宽屏显示导航树与独立详情，窄屏
  改为内联详情，矮屏隐藏次要标题但保留回答流、输入框和 Footer；再次按键返回
  原 DAG/Context 视图。
- `RUNTIME_EVENT_STORE_ENABLED` 默认关闭；显式开启后才加载并注册有界 JSONL
  EventStore，`RUNTIME_EVENT_STORE_MAX_BYTES` 控制单文件轮转上限。存储损坏或
  边界不安全时显示通用警告并退回内存回放；加载严格校验全部轮转记录但内存
  只保留最后 512 条，退出前对已接受事件执行有界刷新。
- 会话标题、预览和消息，以及检查点路径、哈希、原正文与结果正文，在宿主读取
  后立即被不可逆地投影掉；长生命周期 Textual 应用只持有脱敏历史。新增 canary
  回归同时检查快照、渲染输出和应用内部历史表示。
- Time Machine 只观察既有事实，不调用模型、工具或 hook，不发布新事件。Phase 3A 不提供恢复；Phase 3B 安全恢复已于 2026-08-25 另条交付（见下），跨进程主回退仍是 Git。

### 验证与下一阶段

- Ruff lint 与格式检查通过，mypy 对 62 个源文件检查通过；全量 pytest 为
  753 项通过、23 项平台条件跳过。5 个内置离线评测和 wheel 构建也在提交前
  通过，全部验证未调用真实付费模型 API。
- 专用 Windows Sandbox runner 的三轮 workflow、独立 review 和认证仍是最高部署
  门禁；在当前可直接开发的功能中，下一阶段优先设计 idle-only 运行时模型切换，
  并继续阻止跨 Provider/模型私有状态的静默回放。

## 2026-08-24：Web Workbench P9 idle-only 运行时模型切换

### 显式模型目录与原子事务

- 在 `feature/web-runtime-model-switching` 增加
  `WEB_RUNTIME_MODEL_ALLOWLIST`：最多 15 个附加精确模型 ID，启动模型自动加入；
  空白、首尾空格、控制字符、超长值、重复项和未列入白名单的目标全部拒绝。
- 新增纯准备边界 `runtime_models.py`。切换只重建完整 `Settings`，强制 Provider
  不变并保留完整候选目录；不接受浏览器提供 endpoint、凭据或 Provider，也不在
  切换时构造 SDK client 或发送网络请求。
- `switch_model` 要求控制租约、精确 revision、idle、无待审批以及空且未保存的
  活动会话。候选 worker 与快照投影全部在提交前完成；提交时再次校验状态，并在
  同一控制器锁内替换设置/worker、递增 revision 和发布 `model_changed`。任一准备
  或并发检查失败都保留旧运行时。

### Session v5 与绕过防护

- 会话格式从 v4 升级到 v5，新增成对的可选 `runtime_provider` /
  `runtime_model`。版本 1–4 继续严格迁移；未知未来版本拒绝。
- 每个成功 Web turn 原子保存捕获的运行时绑定。已有绑定不能在后续保存中改写；
  重命名、分支、摘要和导入导出均保留绑定，绑定与 Provider 私有状态冲突时拒绝。
- 选择会话时先验证快照绑定，再验证逐消息私有状态。进程内发生过模型切换后，
  含历史但未绑定的旧快照 fail closed，不能通过“先切空会话、再选旧历史”绕过
  门禁；绑定会话只有在 Provider/模型完全一致时才能恢复。

### Web 协议、页面与文档

- 快照新增有界 `available_models`、`can_switch_model` 和会话运行时摘要；实时协议
  新增幂等 `switch_model` 命令及有界 `model_changed` 投影。
- 顶栏模型控件改为原生可访问 `select`，只在实时连接、持有控制且后端能力开放
  时启用；运行/审批、非空会话、无白名单和观察标签页均显示禁用语义。
- 更新 README、环境示例、总体架构、Host Runtime 能力矩阵、Web 开发/运维文档、
  Claude Code 对照和 P9 威胁模型。跨 Provider 切换、自动路由与 fallback 仍不在
  本阶段范围。
- 重新生成 wheel 内生产静态资源及 SHA-256 manifest；P6 四断点视觉基线保持通过。

### 验证与下一阶段

- Ruff lint/格式、mypy（63 个源文件）通过；全量 pytest 为 762 项通过、23 项
  平台条件跳过。新增配置、目录稳定性、v1–v4 迁移、绑定不可改写、运行/非空
  门禁、factory 失败原子性、旧会话绕过、WebSocket 和幂等回归。
- 前端 TypeScript、lint 和 12 项 Vitest 通过；Playwright 5 项 E2E/axe/四断点视觉
  回归通过。5 个内置离线评测全部通过。
- `uv build --wheel` 成功生成 `neil_agent-0.1.0-py3-none-any.whl`，wheel 已确认包含
  新运行时模块、静态资源和 manifest。全部验证未调用真实付费模型 API。
- 最高优先级仍是在专用 Windows runner 完成三轮强制 workflow、独立 review 与
  运行时认证，并手测 `run_command(export_paths)` → `import_guest_export` 全流程。
  Guest 产物导出与二次批准导入的 host 侧实现已于 2026-08-25 合入 `main`。

## 2026-08-25：Web ContextTomography 快照投影

### 运行时

- 新增 `context_projection.py`：`build_host_context_tomography()` 复用 Agent 上下文选择规则。
- `ContextDto.from_tomography()` 投影五层估算、压力、检查点状态与服务端 usage；`WorkbenchSnapshotService.context_dto()` 供快照与 `model_changed` 等事件共用。

### 验证

- `tests/test_context_projection.py` 与 `tests/test_web_workbench.py` 覆盖层序、无正文泄漏与快照一致性。

## 2026-08-25：Web Context 五层断层图 UI

### 前端

- Review 面板新增 `ContextTomographyPanel`：按 TUI 同色带展示五层字符/token 估算、压力等级、轮次裁剪与服务端历史 usage。
- 仅 live 快照且 `context.source === local_estimate` 时渲染；明确标注「本地估算、消息正文不出主机」。

## 2026-08-25：Phase 3B Time Machine 检查点恢复

### 安全恢复入口

- 新增 `time_machine_restore.py`：判定是否可对**最新**任务检查点提供恢复，以及详情面板提示文案。
- `FileSystemTools.prepare_checkpoint_restore()` 在检查点 ID 仍是最新时复用 `/rewind-task` 全量预检与预览。
- `/cockpit --live` Time Machine（`F6`）在空闲且无待审批时，对最新检查点提供 `R` 审批对话框，批准后调用既有 `apply_latest_restore()`；较旧检查点提示使用 Git。

### 验证

- `tests/test_time_machine_restore.py`、`tests/test_checkpoint.py` 与 `tests/test_live_cockpit.py` 覆盖门禁、非最新拒绝与审批恢复闭环。

## 2026-08-25：WSB 认证运维与 Guest Export 协议首步

### 认证运维

- 新增 [`docs/sandbox-certification-runbook.md`](docs/sandbox-certification-runbook.md)：从 CI artifact 到运行时环境变量的逐步清单。
- 新增 [`scripts/windows-sandbox-certify.ps1`](scripts/windows-sandbox-certify.ps1)：封装 `bundle-verify`、独立 `review` 与 `certify`，并打印需 out-of-band pin 的 `review_sha256`。

### Guest export manifest（预览阶段）

- 新增 `sandbox_export.py`：`GuestExportManifest` v1、工作区相对路径校验、敏感/越界拒绝、自哈希 manifest 与无正文预览文案。
- 本阶段仅生成审批用 manifest；导入事务见同日的「Guest Export 导入事务」条目。

### 验证

- `tests/test_sandbox_export.py` 覆盖 digest 绑定、预览不泄漏正文与路径拒绝规则。

## 2026-08-25：Guest Export 导入事务

### 导入事务

- `sandbox_approval.py`：新增 `GuestExportImportBinding`（`guest-export-import` 审批种类），绑定 run、certification、manifest 与逐文件 digest。
- `sandbox_export.py`：新增 `PreparedGuestExportImport` / `PreparedGuestExportImportEntry` 与有界 diff 预览拼装。
- `tools/filesystem.py`：`prepare_guest_export_import()` / `apply_guest_export_import()` — 预览后复核工作区、UTF-8 校验、原子写入、失败回滚。

### 验证

- `tests/test_guest_export_import.py` 覆盖审批绑定稳定性、新建/替换、非 UTF-8 拒绝、批准后竞态拒绝与部分失败回滚。

### 工具与非交互接入

- 新增 `tools/guest_import.py`：`import_guest_export` 工具、`stage()` 暂存认证产物、二次批准预览/应用与 `guest-export-import` 审批绑定解析。
- `ToolRegistry` / `NoninteractiveApprovalBroker` 支持按工具解析审批绑定；`run_command` 同步接入 `sandbox-run-command` 绑定。
- 审计日志在 `import_guest_export` / `run_command` 工具事件上记录 `approval_binding_kind`（仅元数据）。
- `evals/tasks.json` 新增 `guest-export-import-approval` 离线场景。

### run_command 声明式导出

- `run_command` 新增可选 `export_paths`；审批绑定、RunSpec 与 WsbExecutionPlan 同步携带声明路径。
- `sandbox_export_collect.py` 在 sealed export 根目录收集**仅**声明文件并拒绝未知项。
- 命令成功后自动 `build_guest_export_manifest` + `guest_import.stage()`，JSON 结果返回 `guest_export.manifest_sha256` 供 `import_guest_export` 二次批准。

### Web 与非交互审批元数据

- `ApprovalRequestDto` 与 v2 `approval_requests` 条目新增 `binding_kind`（`generic-tool` / `sandbox-run-command` / `guest-export-import`）。
- Web Workbench 审批卡片展示绑定种类标签；`tests/test_web_workbench.py` 覆盖 guest import 绑定投影。

### 文档

- 新增 [`docs/guest-export-import.md`](docs/guest-export-import.md)：端到端流程、工具参数、guest 写入约定、`binding_kind`、限制与手测清单。
- 更新 `architecture.md`、`host-runtime.md`、`non-interactive.md`、`web-workbench-development.md`、`sandbox-certification-runbook.md`、`sandbox-assessment.md`、`claude-code-review.md`、`README.md` 交叉引用。

## 2026-08-26：文档状态同步

### 可视化路线

- `visualization-development.md`：Phase 3B 由「进行中」更正为「已完成」；当时 Phase 4 仍标为可选未开始（已于同日后续提交完成，见下节）。
- `Phase 3A` 条目与 Phase 3B 交付说明对齐；保留「较旧检查点 / 跨进程仍用 Git」约束。

### Web / 开发记录

- `web-workbench-development.md` §19 由「待确认决策」改为「产品决策记录（历史）」；P0–P9 标为已完成。
- `web-workbench-basic-acceptance.md`：P9 模型切换纳入常规回归表述。
- `DevelopmentRecords.md`：2026-08-20 host_runtime 待办、2026-08-23 Phase 3A「3B 延后」、2026-08-24 P9 下一阶段 guest/3B 表述与当前 `main` 对齐。

### 对照审核

- `claude-code-review.md` 后续优先级：guest export host 侧标为已实现；补充 Phase 4 文档链接（Phase 4 已于同日后续提交完成）。

## 2026-08-26：Phase 4 Neural Map

### 交付

- 新增 `neural_map.py`：`NeuralMapProjector`、静态夹具 `build_neural_map_fixture_events()`、纯文本 `render_neural_map_snapshot()`。
- `tool_call` 运行时元数据扩展 `workspace_path`（脱敏相对路径）与 `activity_kind`（read/write/check/other）。
- `/cockpit --live` 使用 `F7` 打开 Neural Map 监控视图；目录热度、时间窗口与风险着色只读展示。
- 单测：`tests/test_neural_map.py`；live cockpit 浏览测试补充 F7 路径。

### 限制

- 仅聚合已记录的工具活动元数据，不扫描工作区或保存文件正文。
- 目录节点上限 48，事件窗口 512；超限时向父目录 rollup。
- `git_stage` 多路径以分号拼接进有界 `workspace_path` 字段。

## 2026-08-26：Phase 4 后文档交叉引用同步

### 收口表述

- `visualization-development.md`：Phase 3 的 Git 回退条目改为**持续约束**；新增「路线收口与跨路线后续项」表，明确编号 Phase 已全部完成。
- `claude-code-review.md`：Phase 4 移入「已实施优化」；「后续优先级」改为跨路线开放项（WSB 手测、guest runner 可选加固、Web 未立项能力）。
- `host-runtime.md`、`README.md`：修正 Phase 4 / F7 Neural Map 的过时描述。

## 2026-08-27：共享 secret denylist 与安全加固路线

### 文档

- 新增 `docs/security-hardening.md`：按 Claude Code 对照审查的建议顺序拆成 6 个批次；本提交只完成批次 1。
- `architecture.md`、`claude-code-review.md`、`host-runtime.md`、`README.md` 交叉引用该路线。

### 批次 1

- 新增 `src/neil_agent/sensitive_paths.py`：目录、文件名、私钥后缀、`.env` / `.env.*`（`.env.example` 除外）。
- `tools/filesystem.py`、`tools/shell.py`（`git_stage`）、`web/service.py`、`sandbox_export.py`、`sandbox_snapshot.py`、`sandbox.py`、`windows_sandbox.py` 删除私有副本，改为调用共享谓词。
- host 读/写/搜索、guest export、Git 暂存、Web 文件树现与沙箱快照同等拒绝 `.ssh` / `.aws` / `id_rsa` / `credentials.json`。
- 本批不解析 `git_diff` 正文（批次 5），不默认打开 OS 沙箱。

## 2026-08-28：质量检查宿主执行预警（批次 2）

### 交付

- `run_quality_check` 预览与工具描述明确「宿主机执行、无 OS 沙箱或网络隔离」；`/permissions` 同步说明。
- `TOOL_WORKFLOW_INSTRUCTIONS` 去掉写入成功后必须跑检查的句；`_with_post_tool_guidance` 不再在 `write_file` / `replace_text` 后催促质量检查。
- 仍保留：用户明确要求或确有必要时才调用检查；若已运行则在最终回答汇总 Command / Exit code / Output。

### 限制

- 质量检查仍在宿主进程以 `shell=False` 执行；未立项 WSB 内跑检查。

## 2026-08-28：审批绑定对齐 v2（批次 3）

### 交付

- 非交互 approve：`NoninteractiveApprovalBroker` 改为 `preflight` + 匹配时 `consume()`，执行点才消费审批；预览漂移时签发新 request 且不提前烧毁原 pending 记录。
- CLI/Web/Agent：批准后、执行前比较当前 `AGENTS.md` 摘要与预览时摘要，变化则拒绝执行并标记 `preview_binding=changed`。
- Git/文件预览截断保留 `Change-ID` 与完整规模；v2 协议 fixture 补齐 `binding_kind` 字段。

## 2026-08-28：Web command_id 与 LLM_BASE_URL（批次 4）

### 交付

- `WorkbenchController`：命令结果缓存绑定 `client_id`；跨客户端 `command_id` 冲突返回 `command_id_conflict`；`unsubscribe` 清理该客户端缓存。
- 浏览器 `protocol.ts`：`command_id` 改为 `crypto.randomUUID()`。
- `Settings.llm_allow_custom_base_url`：默认拒绝非 loopback 的 `LLM_BASE_URL`；须显式 `LLM_ALLOW_CUSTOM_BASE_URL=true`。
- `create_app()` 默认 `PRODUCTION_TRUSTED_HOSTS`（`127.0.0.1`、`localhost`）；测试注入 `TEST_TRUSTED_HOSTS`（含 `testserver`）。

## 2026-08-28：DeepSeek Harness 对照与运行时预设路线

### 文档

- 新增 `docs/runtime-profile.md`：吸收「可组合预设 + 可逆注册 + 明确接缝」，不引入 Cordis、插件市场或任意 Bash。
- 批次 1–3 为提交范围（`RuntimeProfile` / `benchmark-minimal`、可逆 teardown、只读子任务）；4–6 为 goals / skills / 受限 Plan DSL，不提前开工。
- `architecture.md`、`host-runtime.md`、`claude-code-review.md`、`README.md` 交叉引用该路线。

## 2026-08-28：RuntimeProfile 与 benchmark-minimal（批次 1）

### 交付

- 新增 `RuntimeProfile`（`standard` / `benchmark-minimal` / `web-safe`）；`build_host_runtime(..., profile=)` 与 `HostProfile.runtime_profile`。
- `benchmark-minimal`：只读为 `list_directory` / `read_file` / `search_text`；写入模式再加 `replace_text`；无 Git、质量检查、`run_command`、guest import、`set_task_plan`。
- Web 默认 `web-safe`；快照 `security.runtime_profile` 与 CLI `/permissions` 展示预设名。
- `neil-agent-eval` 与 `-p --runtime-profile` 默认/声明 harness；真实 v1 用 `benchmark-minimal`，v2 审批写仍用 `standard`。

## 2026-08-28：可逆注册与 runtime teardown（批次 2）

### 交付

- `ToolRegistry.unregister`、`LifecycleHooks.register` / `JsonlAuditSink.register` / `TaskTracker.register` 返回 disposer；`HostRuntime.close()` 逆序执行。
- `AgentTurnWorker`、非交互 `run_noninteractive` 在 turn 结束后 `close()`；`WorkbenchSnapshotService.replace_host_runtime()` 在 `switch_model` 成功后替换共享 runtime；`WorkbenchController.close()` 关闭 service runtime。
- 测试覆盖 close 清空 registry/hooks、模型切换后旧 registry 不可见。

## 2026-08-28：只读子任务（批次 3）

### 交付

- 新增 `RuntimeProfile.READONLY_SUBTASK` 与 `run_readonly_subtask` 工具（CLI / Web 标准面）；子运行时仅 `list_directory` / `read_file` / `search_text`。
- `subtask.py`：`SubtaskParentState`、`execute_readonly_subtask()`、独立 `subtask_*` 预算/超时/摘要上限；子 `HostRuntime` 必 `close()`。
- `RuntimeEvent` 元数据 `parent_run_id`；Web turn 传入 `run_id`；子事件转发带父链接，不含子任务正文。
- 测试：`tests/test_readonly_subtask.py`（profile 隔离、摘要有界、历史不合并、取消/转发）。
