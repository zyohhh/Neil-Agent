# Neil Agent 开发记录

## 项目信息

- 项目名称：Neil Agent
- 项目类型：本地 Coding Agent / AI 编程助手
- 开发语言：Python
- 开发环境：VS Code
- 项目负责人：Neil
- 文档创建时间：2026 年 7 月
- GitHub 仓库：https://github.com/zyohhh/Neil-Agent
- 当前版本：v0.1.0-dev
- 当前状态：响应式启动工作台已完成，等待真实 DeepSeek 验收

## 文件职责

| 文件 | 当前职责 |
| --- | --- |
| `cli.py` | 通过可注入 Console 接收输入、展示流式文本并处理终端命令 |
| `agent.py` | 管理多轮历史、压缩检查点、受限工具循环和修改后验证工作流 |
| `context.py` | 估算上下文、选择完整历史轮次并保存待应用的压缩结果 |
| `diagnostics.py` | 为 `/doctor` 执行不联网、不泄露密钥的本地环境检查 |
| `instructions.py` | 分层加载、热重载并非覆盖式初始化有界 `AGENTS.md` 项目指令 |
| `evals.py` | 运行默认离线评测和双重显式开启的真实 DeepSeek 验收 |
| `llm.py` | 封装 DeepSeek 请求、可观察重试、流式事件和工具调用解析 |
| `errors.py` | 定义应用、Agent、LLM 和工具层的用户可见异常层级 |
| `schemas.py` | 定义消息、思考内容、工具定义、调用、结果和模型响应结构 |
| `task.py` | 管理单次任务计划、步骤状态和最近一次质量检查快照 |
| `session.py` | 迁移、命名、搜索、原子保存和恢复工作区内的版本化会话快照 |
| `config.py` | 从环境变量或 `.env` 读取并校验 API Key、模型、提示词、重试和运行限制 |
| `registry.py` | 注册工具、生成预览、执行审批检查并统一返回结果 |
| `filesystem.py` | 在工作区内读取、搜索以及原子写入 UTF-8 文件 |
| `shell.py` | 执行固定质量检查、只读 Git，以及需审批的明确路径暂存和本地提交 |
| `__init__.py` | 保存包版本，并把命令行入口转发给 `cli.py` |

## 第一阶段目标

完成一个不包含工具调用的最小聊天闭环：

```text
用户输入
   ↓
cli.py 处理终端交互
   ↓
agent.py 组织最近的多轮消息
   ↓
llm.py 调用 DeepSeek 并返回流式文本
   ↓
cli.py 实时展示回答
   ↓
agent.py 在回答完整结束后保存本轮消息
```

## 2026-07-13：第一个可用版本

### 已完成

- 使用 `pydantic-settings` 从 `.env` 加载配置。
- 默认模型设置为 `deepseek-v4-flash`。
- 使用 DeepSeek Anthropic 兼容地址 `https://api.deepseek.com/anthropic`。
- 使用 `SecretStr` 保存 API Key，避免配置对象意外输出明文密钥。
- 定义不可变的 `Message`、`ToolCall` 和 `ToolResult` 数据结构。
- 实现普通模型请求 `LLMClient.complete()`。
- 实现流式模型请求 `LLMClient.stream()`。
- 将常见的鉴权、限流、超时、连接和 HTTP 错误转换为中文提示。
- 实现 `Agent.chat()` 和 `Agent.stream_chat()`。
- 只在一次回答完整结束后写入消息历史；失败或中断的回答不会污染历史。
- 根据 `MAX_ROUNDS` 仅向模型发送最近的对话轮次，默认保留 20 轮。
- 实现终端多轮聊天，以及 `/help`、`/clear`、`/exit` 命令。
- 修正包入口，现在可以通过 `neil-agent` 启动终端程序。

### 当前配置

将 `.env.example` 复制为 `.env`，然后填写真实 API Key：

```dotenv
DEEPSEEK_API_KEY=your_real_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com/anthropic
DEEPSEEK_MODEL=deepseek-v4-flash
THINKING_ENABLED=false
SYSTEM_PROMPT="You are Neil Agent, a helpful local coding assistant. Give accurate, practical, and concise answers."
MAX_TOKENS=8192
MAX_ROUNDS=20
MAX_CONTEXT_CHARS=120000
MAX_TOOL_ROUNDS=5
REQUEST_TIMEOUT=120
MAX_RETRIES=2
RETRY_BASE_DELAY=1
RETRY_MAX_DELAY=8
WORKSPACE_ROOT=.
COMMAND_TIMEOUT=120
MAX_COMMAND_OUTPUT_CHARS=20000
```

`THINKING_ENABLED` 默认是 `false`，让响应更直接、流式展示更及时。设置为 `true` 后，普通请求和流式请求都会启用 DeepSeek 思考模式。`SYSTEM_PROMPT` 用于调整 Agent 的角色和回答方式，不再需要修改 Python 代码。

### 启动方式

```powershell
Copy-Item .env.example .env
# 编辑 .env 并填写真实的 DEEPSEEK_API_KEY
uv run neil-agent
```

终端内可用命令：

```text
/help   显示帮助
/clear  清空对话历史
/status 显示任务计划、最近检查和本地 Git 状态
/exit   退出程序
```

### 验证结果

- Ruff 代码检查：通过
- Ruff 格式检查：通过
- mypy 类型检查：通过（13 个源文件）
- pytest 离线单元测试：62 项通过
- 单元测试不会发送真实 DeepSeek 请求，也不会消耗 API 额度

2026-07-14，Neil 已使用真实 API Key 完成端到端聊天测试，确认配置加载、网络请求、DeepSeek V4 Flash、流式输出和多轮消息链路均可用。

## 2026-07-14：可配置系统提示词和思考模式

### 已完成

- 增加 `SYSTEM_PROMPT` 配置，可通过 `.env` 修改 Agent 的系统指令。
- 拒绝空白系统提示词，避免向模型发送无效配置。
- 增加 `THINKING_ENABLED` 布尔配置，默认关闭。
- 普通请求和流式请求共享同一套思考模式配置。
- 开启思考模式时发送 Anthropic 兼容的 `thinking` 参数。
- CLI 启动信息会显示当前思考模式是开启还是关闭。
- 增加系统提示词、思考模式和配置校验测试。

## 2026-07-14：只读工具调用闭环

### 运行流程

```text
用户提出项目问题
   ↓
DeepSeek 返回 ToolCall
   ↓
ToolRegistry 校验名称和参数
   ↓
执行 list_directory / read_file / search_text
   ↓
Agent 将 ToolResult 返回 DeepSeek
   ↓
DeepSeek 继续调用工具或生成最终回答
```

### 已完成

- 实现 `list_directory`、`read_file` 和 `search_text` 三个只读工具。
- 使用 `WORKSPACE_ROOT` 限制可访问的项目根目录。
- 解析真实路径，防止利用 `..`、绝对路径或符号链接逃出工作区。
- 默认隐藏 `.env`、`.git`、虚拟环境、缓存目录和常见私钥文件。
- 单文件读取上限为 1 MB，搜索结果上限为 100 条。
- 实现工具注册、重复名称检查、参数绑定、异常捕获和统一 `ToolResult`。
- 将 Anthropic 格式的工具定义、`tool_use` 和 `tool_result` 接入 DeepSeek。
- 思考模式执行工具时保留并回传必要的 thinking 内容。
- 使用 `MAX_TOOL_ROUNDS` 限制单次用户请求的工具循环，默认最多 5 轮。
- 工具循环完整成功后才写入消息历史；失败不会留下半轮历史。
- CLI 启动时显示工作区和已注册的只读工具数量。
- 新增文件权限、注册表、工具循环和 API 消息转换测试。

## 2026-07-14：异常分层与 CLI 可测试性

- 新增 `NeilAgentError` 作为用户可见错误的共同基类。
- `AgentError`、`LLMError` 和 `ToolError` 分别归属编排层、模型层和工具层。
- Agent 不再依赖或误用 `LLMError` 表示自身循环错误。
- CLI 统一捕获 `NeilAgentError`，仍向用户显示简洁的中文错误。
- 移除模块级 `Console` 实例，由 `main()` 创建并传给 `run(console)`。
- 所有 CLI 展示函数均显式接收 Console，便于替换输入和捕获输出。
- 增加异常继承关系和注入式 CLI 交互测试。

## 2026-07-15：受控文件修改

### 审批流程

```text
DeepSeek 请求 write_file / replace_text
   ↓
ToolRegistry 生成 unified diff
   ↓
CLI 展示修改预览并等待用户输入 y/yes
   ├─ 拒绝：不修改文件，将拒绝结果返回模型
   └─ 批准：执行原子写入，再将结果返回模型
```

### 已完成

- 新增 `write_file`，支持创建或完整替换 UTF-8 文本文件。
- 新增 `replace_text`，只有实际匹配数量等于 `expected_replacements` 时才执行。
- 写工具在注册时标记为必须审批，并且必须提供预览函数。
- 未审批、无法审批或用户拒绝时，写工具不会执行。
- CLI 只将明确的 `y` 或 `yes` 视为批准，其他输入默认拒绝。
- 使用 unified diff 展示修改内容，预览最多 20,000 字符。
- 预览包含内容 `Change-ID`；文件在确认后发生变化时拒绝使用旧审批。
- 使用同目录临时文件、刷新磁盘和 `os.replace` 实现原子写入。
- 写入失败时清理临时文件并保留原文件内容。
- 继续复用工作区边界、敏感文件屏蔽和 1 MB 文件大小限制。
- CLI 当前注册 5 个工具，其中 2 个写工具必须人工确认。
- 增加批准、拒绝、精确匹配、敏感路径和模拟写入失败测试。

2026-07-16，Neil 已使用真实 DeepSeek API 验证 `write_file` / `replace_text` 的写入预览、批准和拒绝流程，确认审批闭环可用。

## 2026-07-16：受限命令执行

### 权限规则

- `run_quality_check` 只接受 `pytest`、`ruff`、`mypy` 三个枚举值，并在执行前展示固定命令、工作目录和超时时间。
- 质量检查器的配置或插件可能运行项目代码，因此 `run_quality_check` 必须经过人工批准。
- `git_status` 和 `git_diff` 只读取仓库状态，不修改工作区，可直接执行；同时禁用分页器、fsmonitor、external diff、textconv 和可选锁。
- 不提供任意命令、任意参数、管道、重定向或 Shell 字符串入口。

### 已完成

- 使用当前 Python 解释器的 `-m` 形式运行质量检查，确保复用 Neil Agent 的虚拟环境。
- 所有命令都固定在 `WORKSPACE_ROOT` 中运行，并显式设置 `shell=False` 和空标准输入。
- 子进程只继承运行所需的少量环境变量，不传递 DeepSeek API Key、GitHub Token 等凭据。
- 使用 `COMMAND_TIMEOUT` 限制单次命令时间，默认 120 秒。
- 使用 `MAX_COMMAND_OUTPUT_CHARS` 限制返回给模型的输出，默认 20,000 字符；超长输出保留开头和结尾。
- 命令不存在、超时、启动失败和非零退出码统一转换为工具错误。
- CLI 当前注册 8 个工具，其中文件写入和质量检查共 3 个工具必须人工确认。
- 增加白名单、审批、只读 Git、工作目录、环境净化、超时和输出截断测试。

2026-07-17，Neil 已使用真实 DeepSeek API 验证质量检查的预览、批准和拒绝，以及 `git_status` / `git_diff` 只读调用，确认受限命令闭环可用。

## 2026-07-17：主动质量验证和本地 Git 保存

### 修改后验证

- Agent 在文件写入成功后会明确提醒模型根据修改类型选择合适的 `run_quality_check`，而不是无理由运行全部检查。
- 内置工具工作流会追加到可配置系统提示词之后，不会替换用户在 `.env` 中设置的角色提示词。
- 命令结果统一包含 `Command`、`Working directory`、`Exit code` 和 `Output`，便于模型准确汇总执行情况。
- 最终回答必须说明尝试了哪些检查、退出码和关键结果；用户拒绝审批时必须说明检查未运行。

### 本地 Git 保存

- 新增 `git_stage`，只接受最多 50 个明确的工作区相对文件路径。
- 拒绝 `.`、目录、工作区外路径、受保护目录、`.env` 和常见私钥文件，不提供 `git add -A` 或任意 pathspec。
- 暂存预览同时展示当前状态、已暂存内容、工作区 diff 和未跟踪文本文件内容。
- 暂存预览包含完整文件状态生成的 `Change-ID`；文件或暂存区变化后必须重新批准。
- `git add` 可能运行仓库配置的 clean filter，因此预览会明确提示风险，并始终要求人工批准。
- 新增 `git_commit`，只接受不超过 200 字符的单行提交消息，并在执行前展示暂存区 diff；超长内容会截断，但 `Change-ID` 覆盖完整 diff。
- 本地提交禁用 Git hooks 和 GPG 签名，不支持 amend、空提交、任意参数或推送。
- 暂存完成后提醒模型先调用 `git_diff(staged=true)`；提交完成后明确告知模型不能声称已经推送。
- CLI 当前注册 10 个工具，其中写入、质量检查、暂存和提交共 5 个工具必须人工确认。
- 增加主动验证提示、路径边界、敏感文件、旧审批失效、空暂存区和安全提交参数测试。

2026-07-19，Neil 已使用真实 DeepSeek API 验证修改后主动检查、结果汇总，以及 `git_stage` / `git_commit` 的批准和拒绝流程，确认本地版本保存闭环可用。

## 2026-07-19：可见任务计划和状态

### 任务计划

- 新增 `set_task_plan`，为多步骤开发任务创建 1–5 个简洁步骤。
- 新计划自动把第一步设为 `in_progress`，其余步骤设为 `pending`。
- 新增 `update_task_step`；只有当前进行中的步骤可以完成，完成后自动启动下一步。
- 计划必须按顺序推进，已完成步骤不能重新打开，并拒绝空白、重复或超过 200 字符的步骤。
- 计划只保存在当前 CLI 会话内；`/clear` 会同时清除对话、计划和最近检查。
- Agent 的内置工作流要求模型只为多步骤开发任务创建计划，并尽量把状态更新与相关工具调用合并。
- 计划创建或变化后由 CLI 立即展示，不需要等待最终回答。

### `/status`

- 新增 `/status`，不调用 DeepSeek，也不消耗 API 额度。
- 状态页展示当前计划及 `pending`、`in_progress`、`completed` 状态。
- 记录最近一次 `run_quality_check`，区分 `passed`、`failed` 和因拒绝审批导致的 `not_run`。
- 展示最近检查的命令、退出码和最多 500 字符的关键输出。
- 每次查看状态时通过只读 Git 命令获取最新分支和工作区状态，不依赖旧缓存。
- `/status` 的 Git 快照最多等待 5 秒，避免 Git 偶发卡顿阻塞 CLI。
- CLI 当前注册 12 个工具；两个计划工具只修改内存状态，不需要审批，高风险工具数量仍为 5 个。
- 增加计划限制、顺序推进、即时通知、检查状态解析、清理和 CLI 状态展示测试。

## 2026-07-19：详细实时执行轨迹

### 活动事件

- Agent 在每次模型请求前报告当前阶段、模型轮次、上下文消息数和可用工具数，并在模型返回后列出本轮选择的工具。
- 每个工具调用都会报告执行中、等待批准、完成、失败或跳过；完成和失败事件包含本次工具流程耗时。
- 文件工具显示路径、内容规模、预计替换数、目录条目、搜索范围、查询、匹配数和前几个结果位置。
- 命令与 Git 工具显示实际命令、退出码和有界结果摘要；Git diff 额外显示差异规模与涉及文件。
- 高风险工具把“生成预览”“等待用户批准”“批准后实际执行”分成明确阶段，审批拒绝使用独立的 `skipped` 状态。
- 活动轨迹只展示可观察的输入与结果，不展示模型思维链；文件正文和替换前后文本不会复制到活动详情。
- 所有自由文本详情都会移除终端控制字符、压缩为单行并限制长度，避免输出注入和终端刷屏。

### 终端渲染

- `TerminalRenderer` 使用 Rich 动态 spinner 和持续增长的耗时表示正在等待模型或工具，收到文本、计划或完成事件时自动停止。
- 持久状态使用 `[?]`、`[ok]`、`[-]`、`[!]`，分别表示等待批准、成功、跳过和失败，并在其下缩进展示结构化详情。
- 如果模型先输出文字再调用工具，渲染器会关闭当前回答行；工具结束后的文字会重新显示 `Neil Agent >` 前缀。
- 任务计划更新也会先安全结束当前回答片段，不再依赖散落在 CLI 中的手动换行。
- 增加模型与工具事件顺序、命令结果摘要、文件内容隐藏、审批阶段、动态状态生命周期、流式文本续接和计划插入测试；当前测试总数为 68。

2026-07-20，Neil 已使用真实 DeepSeek API 验证详细执行活动、任务计划的创建与自动推进，以及 `/status`，确认终端实时反馈闭环可用。

## 2026-07-20：本地会话保存和恢复

### 版本化快照

- 新增 `session.py`，使用带 `version: 1` 的严格 JSON 快照保存完整成功对话、任务计划和最近一次质量检查。
- 快照只接受预定义字段，不读取或保存 `.env`、系统提示词、API Key 和其他环境变量。
- 会话保存在工作区的 `.neil-agent/sessions/`；该目录加入 `.gitignore`，并被文件工具和 Git 暂存工具视为受保护目录。
- 单个会话文件最大 25 MB；读取时验证文件大小、会话 ID、文件名、版本、时区、完整消息轮次和任务步骤顺序。
- 文件必须位于真实会话目录中，拒绝路径穿越和符号链接绕过。
- 保存使用同目录临时文件、刷新与 `fsync`、`os.replace` 原子替换；失败时保留旧快照并清理临时文件。

### CLI 会话生命周期

- CLI 启动时创建新的内存会话 ID，但不会写入无内容的空会话。
- 每次完整回答成功后自动保存；失败或被中断的请求不会覆盖最后一次有效快照。
- 新增 `/sessions`，按更新时间列出最多 50 个有效会话、轮数和最近用户请求摘要，不调用 DeepSeek。
- 损坏或未来版本的会话不会阻塞列表，而是被跳过并显示数量。
- 新增 `/resume <session-id>`，只有用户明确指定精确 ID 时才恢复消息、计划和最近检查；启动时不会自动恢复。
- `/clear` 保留已经保存的旧会话，并创建新的会话 ID；继续对话时会保存到新文件。
- `/status` 现在同时显示当前会话 ID；本地保存失败只显示警告，不会丢弃已经生成的模型回答。
- 增加快照往返、环境秘密排除、原子失败、损坏文件、消息轮次、计划恢复、目录保护、CLI 列表/恢复和自动保存测试；当前测试总数为 76。

2026-07-21，Neil 已使用真实 DeepSeek API 验证退出进程后的 `/sessions`、`/resume`，以及恢复会话后继续调用工具，确认跨进程恢复闭环可用。

## 2026-07-21：上下文预算和本地会话管理

### 完整轮次上下文预算

- 新增 `context.py`，以发送给 API 的紧凑 JSON 字符数近似估算上下文；估算包含系统提示词、工具定义、消息文本、思考块、工具调用参数和工具结果。
- 新增 `MAX_CONTEXT_CHARS`，默认值为 `120000`；它与 `MAX_ROUNDS` 同时生效，前者限制近似请求规模，后者限制历史轮数。
- Agent 从最新历史向前选择连续的完整轮次；如果某轮不再适合剩余预算，就连同更早历史一起省略，不会拆散工具调用和对应结果，也不会跳过新轮而保留更旧轮次。
- 预算只裁剪以前已完成的历史。当前用户输入始终发送，正在执行的模型/工具循环也保持完整，因此极端情况下本次请求仍可能超过软预算并由模型 API 返回错误。
- 新增 `/context`，不调用 DeepSeek；它展示预算、系统提示词与工具定义的固定开销、已保存历史、下次请求可带历史以及被省略的轮数。
- `/context` 的“下次请求”统计尚未包含下一条未知的用户输入，因此实际发送历史可能进一步减少；字符估算用于本地保护，不等同于模型精确 token 计费。

### 显式会话清理

- `/sessions` 现在展示所有 JSON 会话文件的总存储占用、有效会话数，以及每个已列出会话的文件大小。
- 新增 `/delete-session <session-id>`：先读取并展示精确 ID、更新时间、轮数、文件大小和最近请求，再要求用户输入 `y` 或 `yes`。
- 空输入、其他回答、终端中断都视为取消；当前活动会话禁止删除，必须先用 `/clear` 或 `/resume` 切换。
- 删除前和实际删除时都会重新校验会话 ID、文件位置、格式和内容；损坏文件仍只计数和跳过，不提供自动清理。
- 程序不会根据预算、数量或存储大小自动删除历史会话，也不增加云同步。
- 增加上下文空状态、连续轮次选择、大型工具结果、实际请求裁剪、存储统计、删除确认/拒绝和当前会话保护测试；当前测试总数为 84。

2026-07-21，Neil 已使用真实 DeepSeek API 验证大型工具结果触发历史省略，以及 `/delete-session` 的批准、拒绝和当前会话保护，确认上下文与本地清理边界可用。

## 2026-07-21：模型请求可靠性和本地自检

### 可观察的有界重试

- Anthropic SDK 的内置重试被关闭，避免底层发生用户看不到的重复等待；重试次数和反馈统一由 `LLMClient` 管理。
- 新增 `MAX_RETRIES`、`RETRY_BASE_DELAY` 和 `RETRY_MAX_DELAY`，默认最多重试 2 次，等待从 1 秒指数增长且单次不超过 8 秒；总请求次数等于首次请求加重试次数。
- 只重试 DeepSeek 限流、HTTP 408、HTTP 5xx、请求超时和连接失败；鉴权、权限、请求格式及其他 4xx 错误立即返回，不浪费时间或额度。
- 对 `Retry-After` 和 `retry-after-ms` 响应头进行解析，但服务端建议等待仍受 `RETRY_MAX_DELAY` 约束，避免终端长时间无响应。
- 每次重试前通过现有活动渲染器显示固定的安全原因、当前次数和等待秒数；等待结束后切换为“重试模型请求”的动态状态。
- 流式请求只有在尚未向终端输出正文时才允许重试；一旦用户已经看到文本，后续错误直接报告，避免重复片段和难以判断的部分回答。
- 重试等待可被 `Ctrl+C` 中断，失败请求和取消请求仍不会写入会话历史。

### `/doctor` 本地诊断

- 新增 `diagnostics.py` 和 `/doctor`，检查已加载配置、工作区读写能力、本地会话目录和 Git 仓库状态，全程不调用 DeepSeek。
- 配置检查只显示“API Key 已配置（值已隐藏）”，不会读取到报告、终端详情或异常文本中；API 地址使用非 HTTPS 时给出警告。
- 会话检查复用严格的快照读取边界，报告有效文件数、总占用和损坏文件数，不自动修复或删除。
- Git 检查复用受限的只读状态命令，只报告仓库是否可访问及工作区是否干净，不展示完整文件列表。
- 诊断结果区分通过、警告和错误；Git 不可用只降低相关功能，不阻止基础聊天。
- 增加 SDK 重试关闭、连接恢复、指数等待、服务端等待上限、鉴权/格式错误、重试次数耗尽、流式重复保护、配置校验、密钥隐藏和诊断降级测试；当前测试总数为 95。

本阶段没有故意制造真实 DeepSeek 限流或网络故障；重试上限、反馈顺序和流式重复保护已通过可控 SDK 异常测试验证，真实外部异常仍保留为人工验收项。

## 2026-07-21：项目指令和长会话连续性

### 根目录 `AGENTS.md`

- 新增 `instructions.py`，启动时只检查 `WORKSPACE_ROOT/AGENTS.md`；不存在或空文件不会阻止启动。
- 文件固定上限为 32768 字节，只接受真实普通文件和 UTF-8 文本；拒绝符号链接、越过根目录的解析结果、无效编码、Unicode 控制/格式字符和读取过程中发生的路径变化。
- 有效内容经过换行规范化和首尾空白清理，再以明确边界追加到系统上下文；内置工具与压缩规则仍位于其后。
- 项目指令只存在于当前进程的系统上下文，不作为用户/助手消息，也不写入版本 1 会话快照；恢复会话时使用当前启动时读取的文件。
- 新增 `/instructions`，只显示状态、绝对来源、字节数、字符数和固定错误原因，不显示指令正文、系统提示词或其他隐藏配置。
- 启动页会提示指令已加载或文件无效；文件在进程运行期间发生变化不会自动热加载，需要重启后生效。

### 显式 `/compact`

- `/compact` 只在用户明确输入命令后运行，至少需要 3 个完整轮次；默认把较早轮次总结为一个有标记的压缩检查点，并原样保留最近 2 个完整轮次。
- 压缩输入使用旧消息的 API JSON 表示，保留文本、工具调用、参数和结果的结构；单个旧轮次最多送入 20000 字符，超出时保留前后内容并加入明确截断标记。
- 当旧历史无法放入一次 `MAX_CONTEXT_CHARS` 预算时，Agent 会按完整轮次分批生成滚动摘要；极端大轮次会进一步按当前预算有界缩减，单次 `/compact` 最多发起 8 次模型请求。
- 摘要要求保留目标、约束、决定、文件、工具结果、验证和未完成事项，最大接受 8000 字符；模型返回空摘要、超限摘要或候选历史未实际变小时拒绝替换。
- Agent 先生成不可变的候选压缩历史，不修改当前消息；CLI 先使用原子会话保存写入候选，成功后才应用到内存。模型失败、取消、过期结果或磁盘保存失败都会保持原历史。
- 压缩检查点在 `MAX_ROUNDS` 清理和下一次请求选择中被识别为固定轮次，在字符预算允许时优先保留；`/clear` 或下一次成功 `/compact` 才会替换它。
- 压缩过程复用模型重试活动，结束后显示总结轮数、完整保留轮数、模型请求次数和近似减少字符数；任务计划和最近质量检查保持不变。

### 回归场景

- `evals/tasks.json` 从空列表扩展为 5 个可执行人工场景，覆盖根指令遵循、不安全指令拒绝、压缩后连续性、压缩失败原子性，以及重试/审批/恢复一致性。
- 新增评测结构检查，以及指令编码、大小、控制字符、条件式符号链接、正文隐藏、系统上下文注入、会话排除、工具轮次保留、超大轮次缩减、固定检查点、过期结果和保存失败测试。
- 当前自动化结果为 111 项通过；1 项符号链接用例在当前 Windows 未提供创建权限时按平台条件跳过。

## 2026-07-21：分层项目指令、会话导航和可运行评测

### 分层项目指令生命周期

- 指令目标取启动目录；启动目录不在 `WORKSPACE_ROOT` 内时安全回退到根目录。加载器沿根目录到目标目录的真实祖先链查找 `AGENTS.md`，由外到内组合，更内层规则只作用于其目录和后代。
- 单文件仍限制 32768 字节，完整生效链新增 65536 字节累计上限；任一文件是符号链接、越界、编码无效、包含控制字符或使累计超限时，整条候选快照无效。
- `/instructions` 现在按顺序展示目标、生效来源、各文件大小和作用域，但继续隐藏正文与完整系统提示词。
- 新增 `/reload-instructions`；Agent 从基础提示词重新构建系统上下文，不清除历史。候选链完全有效后才替换，失败时继续使用旧快照。
- 新增 `/init`；它不调用模型，只读取有界的 `pyproject.toml`、`package.json`、`Cargo.toml` 或 `go.mod` 标记，生成短初稿和 unified diff。
- `/init` 只接受明确的 `y` / `yes`，批准后使用独占创建再次检查根 `AGENTS.md`，不会覆盖启动后或预览后出现的文件。

### 会话格式 v2 与本地搜索

- 当前快照写入版本 2 并新增标题；首次成功回答后从第一条用户请求确定性生成默认标题，不调用模型，也不把系统提示词写入快照。
- 旧版本 1 快照继续严格读取，在下一次保存或重命名时迁移为版本 2；未知未来版本继续作为不兼容文件跳过。
- 新增 `/rename-session <标题>`，修改当前会话标题；空标题、超过 80 个字符或包含控制/格式字符的标题会被拒绝。
- `/sessions [关键词]` 在本地按标题、会话 ID 和最近请求进行不区分大小写的包含搜索，同时显示全部有效数量与匹配数量，不调用模型。

### 可执行评测

- 新增 `neil-agent-eval` 命令，把 `evals/tasks.json` 的 5 个场景接入确定性执行器；默认使用假模型和临时工作区，不读取 API Key、不联网、不修改真实项目。
- 离线评测覆盖指令注入与会话排除、不安全指令拒绝、压缩连续性、压缩失败原子性，以及注入一次 SDK 连接异常后的真实重试分支、写入拒绝与会话恢复。
- 未注册执行器的场景会失败并返回非零退出码，避免评测文件退化为无人执行的文字清单。
- 真实模式必须同时提供 `--real-deepseek --confirm-api-cost`，只在临时工作区验证项目指令、`read_file`、压缩与新 Agent 恢复；自然重试只观察和报告，不主动制造外部故障。
- 当前自动化结果为 125 项通过；1 项符号链接用例在当前 Windows 未提供创建权限时按平台条件跳过。

## 2026-07-21：响应式启动工作台

- 原有模型、思考模式、工作区、会话和工具数量的逐行输出合并为一个 Rich 边框面板，在第一次输入提示前建立清晰的运行上下文。
- 面板使用 Neil Agent 自有的绿色视觉标识和“本地 Coding Agent 已准备就绪”状态，不复用 Claude Code 的名称、图形或品牌文案。
- 启动信息现在同时展示可用工具数、需要审批的工具数，以及项目指令的生效来源数量和总大小。
- 项目指令缺失时直接提示 `/init`，空文件提示 `/reload-instructions`，无效文件显示固定安全原因；任何状态都不展示指令正文、系统提示词或 API Key。
- 模型名、工作区路径和会话 ID 使用 Rich 纯文本节点，不解析运行时 markup；长路径和窄终端会自动折行。
- 面板底部提供 `/help`、`/doctor`、`/instructions` 三个起步入口，以及 `Ctrl+C` 和 `/exit` 提示；原有命令和对话行为不变。
- 新增真实 Rich Console 渲染测试，覆盖正常宽度、52 列窄终端、有效/缺失/无效指令状态和正文隐藏；当前自动化结果为 128 项通过，1 项 Windows 符号链接条件跳过。

## 2026-07-22：作用域指令、可移植会话与双重上下文预算

### 文件作用域指令

- 新增 `ProjectInstructionManager`，文件工具每次根据实际 `path` 解析目标目录，并沿工作区根目录到目标重新计算有效 `AGENTS.md` 链。
- 生效链变化时，Agent 先更新系统上下文并返回“本次文件操作未执行”的工具结果；模型在下一轮看到新规则后才能重新调用，避免首次访问越过内层规则。
- 作用域文件无效、目标越界或父目录不存在时直接拒绝文件操作；相同有效提示词仅更新内部目标，不额外消耗工具轮次。
- `/instructions` 和 `/reload-instructions` 跟随当前实际作用域，项目规则继续不写入会话快照。

### 可执行评测和本地质量检查

- `neil-agent-eval` 新增可重复的 `--task <id>`、`--format text|json` 和每场景毫秒耗时；未知或重复场景明确失败。
- JSON 报告包含固定的汇总与结果字段，便于脚本和 CI 消费；耗时使用可注入时钟测试，不依赖真实等待。
- `run_quality_check` 增加固定的 `eval` 枚举，执行 `python -m neil_agent.evals --format json`；它仍需要审批，且永不隐式添加真实 API 参数。

### 会话导航和安全交换

- `/sessions` 新增 `--page`、`--page-size`、`--sort updated|title`、`--order asc|desc` 和 `--state all|planned|failed|compacted`，所有搜索、排序和筛选都在本地完成。
- 会话摘要增加有计划、检查失败、含压缩检查点三个状态标记；分页结果同时保留有效数、匹配数和总占用统计。
- `/export [id]` 预览后把严格版本化信封独占创建到 `.neil-agent/exports/`；信封只包含版本 2 会话，不包含 API Key、环境变量、系统提示词或项目指令。
- `/import <文件名>` 只接受导出目录中的真实普通文件，验证文件名、25 MB 上限、版本、完整消息、重复 ID 和工作区边界；批准后再次校验 SHA-256 与目标不存在，拒绝预览后变化和覆盖。

### Token 预算适配

- 新增可选 `MAX_CONTEXT_TOKENS`；不配置时原有 `MAX_CONTEXT_CHARS` 行为完全保留，两者配置时必须同时满足。
- 当前模型无关估算对 ASCII 使用约 4 字符/token，对非 ASCII 使用约 1 字符/token；这是保守软预算，不声称等同 DeepSeek tokenizer 或计费数据。
- 普通历史选择、压缩请求和 `/context` 均接入 token 统计，同时继续按完整对话轮次裁剪。
- 自动化结果为 138 项通过；1 项 Windows 符号链接用例在当前环境无创建权限时跳过。真实 DeepSeek 验收未自动执行，仍需用户显式提供 `--confirm-api-cost`。

## 2026-07-22：Claude Code 官方文档对照审核

- 新增 `docs/claude-code-review.md`，按项目指令、权限、沙箱、会话、检查点、结构化输出与 hooks 对照官方行为，区分已对齐、明确差异和后续能力。
- 项目指令提示明确标为非可信仓库上下文，当前用户请求优先；文件、命令和 Git 权限仍只由代码控制。
- 新增 `/permissions`，展示直接与逐次审批工具、工作区、敏感路径、命令、网络和 OS 沙箱现状，不调用模型也不修改权限。
- 新增 `/branch [标题]`，完整复制消息、任务计划和最近检查到新会话 ID 后切换，原会话保持不变。
- `/compact [关注点]` 支持有界摘要关注点，并在覆盖当前快照前创建可恢复的“压缩前”分支；压缩不再是完整对话的唯一副本。
- 自动化结果为 140 项通过；1 项 Windows 符号链接用例按环境条件跳过。

## 下一阶段计划

1. 使用真实 DeepSeek API 验证作用域刷新重试、`/branch`、带关注点压缩及压缩前恢复，并执行一次 `run_quality_check(eval)` 的批准与拒绝流程。
2. 定义一次性非交互运行的 `text`、`json` 和 `stream-json` 事件协议，先实现只读工具与显式退出码。
3. 设计类型化生命周期 hooks，优先提供审计、拒绝和有界附加上下文，不接受任意 shell 字符串。
4. 调研 DeepSeek 官方 tokenizer 或 token 计数能力；不可用时保留当前模型无关估算并强化误差提示。
5. 在开放更通用命令前评估 Windows/Linux 的 OS 级文件与网络沙箱，当前固定命令白名单保持不变。
